/* ==========================================================================
   quizzes-3.js — the question bank, part 3.

   APPEND ONLY. Ids are "<chapter-id>#<index>" and the index is the array
   position; it keys a learner's spaced-repetition schedule. Reordering or
   deleting silently reassigns somebody's review history.

   `why` is shown on correct answers too.
   ========================================================================== */

window.QUIZZES = Object.assign(window.QUIZZES || {}, {

  /* ------------------------------------------------------- chapter 09 --- */
  "09-databases-and-mysql": [
    { q: "Why should a MySQL table use utf8mb4 rather than utf8?",
      a: ["MySQL's utf8 is limited to 3 bytes per character and cannot store emoji or some CJK characters",
          "utf8 is slower",
          "utf8 does not support accented characters",
          "utf8 is deprecated and removed in MySQL 8"],
      c: 0,
      why: "MySQL's 'utf8' is an alias for utf8mb3 — a maximum of three bytes, so anything outside the Basic Multilingual Plane cannot be stored and is either rejected or silently truncated. utf8mb4 is real UTF-8. Set it on the column, the connection AND the PDO DSN; the connection is the one people forget." },

    { q: "Which storage engine should a new MySQL table use, and why?",
      a: ["InnoDB — transactions, foreign keys, row-level locking and crash recovery",
          "MyISAM — faster reads",
          "MEMORY — everything is cached anyway",
          "It makes no difference in MySQL 8"],
      c: 0,
      why: "MyISAM has no transactions, no foreign keys, locks the entire table on write and can corrupt on crash. You still meet it in legacy Italian gestionali, and it is the first thing to check when one 'randomly loses data' or stalls under concurrent writes." },

    { q: "How should a monetary amount be stored in MySQL?",
      a: ["DECIMAL, or an integer number of cents",
          "FLOAT, for speed",
          "DOUBLE, for precision",
          "VARCHAR, to preserve formatting"],
      c: 0,
      why: "FLOAT and DOUBLE are binary approximations: 0.1 + 0.2 is not exactly 0.3, so totals drift by a cent and an invoice stops adding up. DECIMAL is exact decimal arithmetic; integer cents is exact everywhere, including in JavaScript on the way to the browser." },

    { q: "Why is a random UUIDv4 a poor choice for an InnoDB primary key?",
      a: ["The primary key determines physical row order, so random keys make every insert land mid-table and fragment the index",
          "UUIDs cannot be indexed",
          "UUIDs are too long for a primary key",
          "InnoDB requires integer primary keys"],
      c: 0,
      why: "InnoDB clusters the table on the primary key, so sequential keys append and random ones insert in the middle, causing page splits and fragmentation. Either keep an auto-increment key internally and expose a UUID alongside it, or use UUIDv7, which is time-ordered." },

    { q: "How should an Italian codice destinatario or CAP be stored?",
      a: ["As a string — they are identifiers, and integers would drop leading zeros",
          "As an INT, since they are numeric",
          "As a DECIMAL",
          "As an ENUM of valid values"],
      c: 0,
      why: "You never do arithmetic on them, and a codice destinatario of '0000000' becomes 0 as an integer. Anything that is an identifier rather than a quantity — partita IVA, CAP, VAT numbers, phone numbers — is CHAR or VARCHAR." },

    { q: "You declare a foreign key from order_lines to orders. What must be decided deliberately?",
      a: ["The ON DELETE behaviour, since CASCADE, RESTRICT and SET NULL mean very different things",
          "Whether the key is stored as an integer or a string",
          "Whether the index is created before or after the data",
          "Which storage engine each table uses"],
      c: 0,
      why: "CASCADE deleting an order takes its lines, which is usually right, while the same choice on a customer with orders can silently destroy financial history. Not choosing means accepting whatever the default is, which is a decision made by not thinking about it." },

    { q: "Why is NOT NULL a better default than nullable for a new column?",
      a: ["Every nullable column becomes a branch in every query and every piece of code that reads it",
          "Nullable columns cannot be indexed in InnoDB",
          "NULL values are stored inefficiently",
          "Nullable columns break foreign key constraints"],
      c: 0,
      why: "NULL propagates through comparisons, breaks NOT IN, needs COALESCE in aggregates and forces a null check in PHP. Allow it where absence is a real, distinct state; do not allow it merely because you have no value to hand right now." },

    { q: "Why must the unit price be copied onto the order line rather than read from the product table?",
      a: ["The order is a historical fact, and a later price change must not rewrite it",
          "The join would be too slow at scale",
          "The product row may be deleted",
          "It avoids a foreign key constraint"],
      c: 0,
      why: "This is not denormalisation for speed, it is recording a fact. What the customer paid is fixed at the moment of purchase, and re-deriving it from today's catalogue produces invoices that contradict the money that actually moved." },

    { q: "Setting the column to utf8mb4 is not enough for emoji and accents to survive. What else must be set?",
      a: ["The connection charset, in the DSN or immediately after connecting",
          "The server's default collation only",
          "The PHP internal encoding with mb_internal_encoding",
          "The HTTP response's Content-Type header"],
      c: 0,
      why: "Bytes are converted on the way in and out, so a latin1 connection mangles them regardless of how the column is defined. Set it on the column, the connection and the DSN, and the mojibake bug that appears months later never happens." },

    { q: "When is it right to stop normalising?",
      a: ["When a further split would only serve theory and the queries it forces are worse",
          "At third normal form, always, without exception",
            "As soon as any join appears in a query",
          "Once the table exceeds a million rows"],
      c: 0,
      why: "Third normal form is a sound default rather than a rule of nature. Splitting a two-field address into its own table to satisfy a diagram buys nothing and costs a join in every query, and knowing when to stop is what separates a design from a exercise." },
  ],

  /* ------------------------------------------------------- chapter 13 --- */
  "13-http-and-psr": [
    { q: "Which HTTP methods are idempotent?",
      a: ["GET, HEAD, OPTIONS, PUT and DELETE — but not POST or PATCH",
          "Only GET and HEAD",
          "All of them except DELETE",
          "GET, POST and PUT"],
      c: 0,
      why: "Idempotent means N identical requests have the same effect as one. It decides what a client, proxy or flaky mobile connection may safely retry: a retried PUT is harmless, a retried POST creates a second order. That is why payment endpoints take an idempotency key." },

    { q: "A request parses correctly as JSON but fails your validation rules. Which status code?",
      a: ["422 Unprocessable Content", "400 Bad Request", "409 Conflict", "500 Internal Server Error"],
      c: 0,
      why: "400 means the request itself was malformed — broken JSON, a missing required header. 422 means it was well-formed but semantically wrong. Laravel returns 422 for validation failures and clients depend on that distinction to know they can render per-field errors." },

    { q: "What is the difference between 401 and 403?",
      a: ["401 means the server does not know who you are; 403 means it does and the answer is still no",
          "401 is for APIs, 403 is for web pages",
          "403 means the resource does not exist",
          "They are interchangeable"],
      c: 0,
      why: "401 invites the client to authenticate — retrying with credentials makes sense. 403 says authentication succeeded and authorisation failed, so retrying with the same credentials is pointless. Returning the wrong one sends clients into useless retry loops." },

    { q: "Why must a GET request never change server state?",
      a: ["Crawlers, prefetch and link scanners follow GETs automatically, so state changes fire without a user acting",
          "GET requests cannot carry a body",
          "GET responses are always cached",
          "HTTP forbids it at the protocol level"],
      c: 0,
      why: "A URL like /orders/5/delete works fine until a crawler, browser prefetcher, or a corporate mail scanner walks your admin panel and deletes everything — and the logs will show the request came from the logged-in user." },

    { q: "PSR-7 HTTP message objects are immutable. What does that mean in practice?",
      a: ["Methods like withHeader() return a NEW object — discarding the return value silently does nothing",
          "They cannot be serialised",
          "Headers can only be set in the constructor",
          "They are read-only after the response is sent"],
      c: 0,
      why: "Calling $response->withHeader('X-Foo', 'bar') without reassigning is a no-op, and it fails silently — no error, the header simply never appears. This is the single most common PSR-7 bug and the reason middleware always reads `$request = $request->withAttribute(...)`." },

    { q: "What is the difference between a safe HTTP method and an idempotent one?",
      a: ["Safe means it changes nothing; idempotent means repeating it has the same effect as doing it once",
          "They are two words for the same property",
          "Safe applies to reads and idempotent to writes",
          "Safe means it can be cached; idempotent means it can be retried"],
      c: 0,
      why: "GET is both. DELETE is idempotent but not safe: it changes state, and deleting twice leaves the same result. The distinction decides what a proxy may prefetch and what a client library may retry after a timeout." },

    { q: "A client sends an update with a version that is no longer current. Which status code should it receive?",
      a: ["409 Conflict, returning the current state alongside it",
          "400 Bad Request",
          "412 Precondition Failed with no body",
          "422 Unprocessable Content"],
      c: 0,
      why: "The request was well formed and semantically valid; it simply lost a race. Returning the current representation lets the client show what changed rather than making the user reload and guess, which turns a dead end into a resolvable conflict." },

    { q: "What does PSR-15 define?",
      a: ["A middleware and request-handler contract that lets middleware from any library interoperate",
          "The immutable HTTP message objects themselves",
          "A dependency injection container interface",
          "The HTTP client interface for outbound requests"],
      c: 0,
      why: "PSR-7 gives the messages and PSR-15 gives the pipeline: a handler that returns a response, and middleware that receives a request and the next handler. Together they are why a Slim application can use middleware written for another framework." },

    { q: "What should an API return after successfully creating a resource?",
      a: ["201 Created, with a Location header pointing at the new resource",
          "200 OK, with the created resource in the body",
          "202 Accepted, with the resource id",
          "204 No Content, since the client already has the data"],
      c: 0,
      why: "201 says something now exists, and Location says where. Returning the representation in the body as well is common and useful. Answering 200 for a creation loses the distinction that lets a client tell a create from an idempotent no-op." },

    { q: "What does statelessness require of a REST API?",
      a: ["Each request carries everything needed to process it, with no server-side conversation state",
          "The server keeps no data at all between requests",
          "Every response must be cacheable",
          "Clients must not store any state either"],
      c: 0,
      why: "Application state lives with the client, resource state lives in the database, and no request depends on a previous one having been handled by the same process. That is what lets you put four servers behind a load balancer and stop thinking about it." },
  ],

  /* ------------------------------------------------------- chapter 24 --- */
  "24-testing": [
    { q: "What is the difference between a stub and a mock?",
      a: ["A stub returns canned values; a mock also carries expectations about how it was called and fails if they are not met",
          "A mock is faster",
          "A stub is for interfaces, a mock for classes",
          "They are two names for the same thing"],
      c: 0,
      why: "Stub = state verification (what came back). Mock = behaviour verification (that it was called, how many times, with what). Use mocks where the interaction itself is the requirement — 'charge the card exactly once' — and stubs where you just need a value." },

    { q: "Why is substituting SQLite for MySQL in tests a bad idea?",
      a: ["Different type handling, date functions and strict-mode behaviour mean tests pass that would fail against MySQL",
          "SQLite is too slow",
          "SQLite cannot be used from PHP",
          "It is fine — an in-memory database is equivalent"],
      c: 0,
      why: "You would be testing against a database you do not deploy, which hides precisely the bugs integration tests exist to catch. Testcontainers starts a real MySQL in Docker for the run, so CI matches production." },

    { q: "What does 90% line coverage actually prove?",
      a: ["That 90% of lines executed during the test run — not that their behaviour was verified",
          "That 90% of bugs are caught",
          "That the test suite is well designed",
          "That 90% of requirements are tested"],
      c: 0,
      why: "Coverage measures execution, not verification: a test with no assertions covers plenty of lines and proves nothing. Mutation testing (infection/infection) is the honest counterpart — it changes your code and counts how many mutants your tests fail to notice." },

    { q: "Which is the strongest reason a bug fix should begin with a failing test?",
      a: ["It proves the bug exists and then proves it cannot silently return",
          "It increases coverage",
          "It is required by TDD",
          "It documents the fix for the changelog"],
      c: 0,
      why: "A test written after the fix might pass for the wrong reason — you never saw it fail. Watching it go red first proves it actually exercises the bug, and once green it is a permanent regression guard on exactly that behaviour." },

    { q: "Why is asserting 'the service called save() twice' usually a bad test?",
      a: ["It asserts implementation rather than behaviour, so it breaks on every refactor while catching no real bugs",
          "Mocks cannot count calls",
          "It is too slow",
          "save() should be private"],
      c: 0,
      why: "Tests coupled to how the code works — rather than what a caller can observe — fail whenever you restructure working code. They get deleted eventually, and they take the team's trust in the test suite with them." },

    { q: "Which collaborators should be replaced with test doubles, and which should be used for real?",
      a: ["Mock the boundaries you do not own, and use real objects inside your own domain",
          "Mock every dependency, so each test covers exactly one class",
          "Use real objects everywhere, including third-party APIs",
          "Mock anything that touches the filesystem or the network only"],
      c: 0,
      why: "Mocking your own value objects and entities produces tests that assert how the code is written and break on every refactor. The payment gateway and the mail service are outside your control, and those are exactly what a double is for." },

    { q: "What is the standard way to keep database tests fast and independent of each other?",
      a: ["Run each test in a transaction that is rolled back afterwards",
          "Truncate every table before each test",
          "Recreate the schema before each test",
          "Run tests in a fixed order so state accumulates predictably"],
      c: 0,
      why: "Rollback is far faster than truncation and leaves nothing behind, so tests can run in any order. The one thing to watch is code that commits or uses its own connection, which escapes the rollback and reintroduces the leakage you were avoiding." },

    { q: "What does a mutation score tell you that line coverage does not?",
      a: ["Whether the tests would actually fail if the code were wrong",
          "Which lines are executed most frequently",
          "How much of the code is reachable from the public API",
          "Whether the tests run in a reasonable time"],
      c: 0,
      why: "Coverage measures execution, and a test with no assertions covers plenty. Mutation testing changes an operator or a boundary and checks that something goes red, which is the only automated answer to 'are these tests verifying anything'." },

    { q: "Why are test names worth spending time on?",
      a: ["They document the rules, so a failure names the behaviour that broke",
          "They determine the execution order of the suite",
          "They are used to generate coverage reports",
          "Long names make the suite easier to filter"],
      c: 0,
      why: "test_a_shipped_order_cannot_be_cancelled tells a reader the rule without opening the file, and its failure message states exactly what regressed. testOrder1 tells them nothing and makes the suite a maze at precisely the wrong moment." },

    { q: "What distinguishes an integration test from a unit test in a PHP application?",
      a: ["It exercises several components together, usually including a real database or framework kernel",
          "It runs in a browser rather than in the console",
          "It is written by a tester rather than a developer",
          "It uses mocks rather than real objects"],
      c: 0,
      why: "The distinction is scope, not tooling. A unit test isolates a piece of logic and runs in milliseconds; an integration test proves the pieces actually fit, which is where mapping errors, transaction bugs and configuration mistakes live." },
  ],


  /* ------------------------------------------------------- chapter 23 --- */
  "23-architecture": [
    { q: "What does the dependency rule in a layered architecture require?",
      a: ["Dependencies point inward, so the domain can be compiled without the framework",
          "Each layer may depend only on the layer directly below it",
          "The domain layer owns the database schema",
          "Every layer depends on a shared kernel"],
      c: 0,
      why: "The test is concrete: could the domain classes be used in a console script with no framework loaded? If they import the ORM's base model or the HTTP request, the answer is no, and the business rules are now tied to a vendor's release schedule." },

    { q: "What is an anaemic domain model?",
      a: ["Entities that are only getters and setters, with all the rules in a large service class",
          "A model with too few properties to represent the domain",
          "A domain layer with no interfaces",
          "A model that lacks database persistence"],
      c: 0,
      why: "The data and the rules that protect it live apart, so nothing prevents another service from setting a state directly and skipping the rule. It is not always wrong, but calling it 'domain-driven design' because there is an Entity folder is the mistake." },

    { q: "Which single DDD tactic gives the highest return in an ordinary PHP codebase?",
      a: ["Value objects, such as Money, Email and PartitaIva",
          "Event sourcing",
          "The repository pattern",
          "Bounded contexts with separate databases"],
      c: 0,
      why: "A Money type that cannot be added to an Ordine and validates its own currency eliminates a class of bug permanently, and it costs one small class. It also documents the domain in the type system, where nobody can skip reading it." },

    { q: "What does the rule 'one transaction, one aggregate' tell you when a use case must modify two aggregates atomically?",
      a: ["The aggregate boundary is probably drawn in the wrong place",
          "The two aggregates should be merged into one entity",
          "You need a distributed transaction coordinator",
          "The use case should be split into two endpoints"],
      c: 0,
      why: "An aggregate is meant to be the consistency boundary. Needing two inside one transaction is a signal that the invariant you are protecting actually spans both, so either the boundary moves, or the second change becomes eventually consistent through an event." },

    { q: "What does CQRS ask you to separate, in its plain form?",
      a: ["The write path through the domain from the read path, which may query SQL directly",
          "The read database from the write database, always",
          "Commands from events",
          "The API from the domain"],
      c: 0,
      why: "Writes need invariants, so they go through the model. Reads need shapes and speed, so forcing a report through aggregates is pure cost. That is the whole idea, and it requires neither a second database nor a message bus to be worth doing." },

    { q: "What is the relationship between CQRS and event sourcing?",
      a: ["They are independent; CQRS is commonly used without event sourcing",
          "Event sourcing requires CQRS and vice versa",
          "CQRS is a special case of event sourcing",
          "They are two names for the same pattern"],
      c: 0,
      why: "Event sourcing stores state as a sequence of events, which is a much larger commitment involving versioning, replay and projections. Conflating them is how a first project acquires an event store it did not need and cannot query." },

    { q: "What does ubiquitous language mean in practice?",
      a: ["The code uses the business's own words, untranslated, including Italian domain terms",
          "All code and comments are written in English",
          "The domain has a glossary maintained separately from the code",
          "The API uses the same field names as the database"],
      c: 0,
      why: "If the business says fattura, ddt and nota di credito, then classes called Fattura and NotaDiCredito remove a translation layer where misunderstandings live. Renaming them to Invoice and CreditNote sounds tidier and quietly loses the distinctions." },

    { q: "How much architecture does a three-month CRUD project for one client deserve?",
      a: ["Value objects and a clear service layer, without hexagonal ports or CQRS",
          "The full hexagonal structure, since it costs nothing to set up",
          "None, since the framework's conventions are sufficient",
          "Event sourcing, so the audit trail is free"],
      c: 0,
      why: "Architecture buys optionality, and optionality has a price paid on every file you open. Matching the investment to the expected lifetime and rate of change is itself the senior skill, and saying so is a better interview answer than naming patterns." },

    { q: "In hexagonal architecture, what is a port?",
      a: ["An interface owned by the domain describing what it needs from the outside world",
          "The HTTP entry point of the application",
          "A network port the service listens on",
          "The database connection abstraction provided by the framework"],
      c: 0,
      why: "The domain declares 'I need to store orders' as an interface, and an adapter in the infrastructure layer implements it with Doctrine or a file or a fake. The direction of ownership is the whole point, and it is the same idea as dependency inversion." },

    { q: "Why must architectural boundaries be enforced by a tool rather than by agreement?",
      a: ["Under deadline pressure an unchecked boundary is crossed once and then permanently",
          "Tools can refactor the code automatically",
          "Agreements cannot be documented in the repository",
          "Static analysis is faster than code review"],
      c: 0,
      why: "The first violation is always urgent and reasonable-sounding, and the second cites the first as precedent. A failing build removes the negotiation, which is why deptrac or an ArchUnit-style test is worth more than a diagram on a wiki." },
  ],

  /* ------------------------------------------------------- chapter 25 --- */
  "25-observability": [
    { q: "What makes a log line queryable?",
      a: ["A stable message with the variable parts in a structured context array",
          "Including as much detail as possible in the sentence",
          "Writing one line per request rather than per event",
          "Using a consistent timestamp format"],
      c: 0,
      why: "Interpolating the order id into the message creates a million distinct messages, so you cannot count or group them. A fixed message plus context lets you ask how many times this happened, and then filter to one order without changing the query." },

    { q: "Where should a containerised PHP application write its logs?",
      a: ["As JSON to stdout, letting the platform collect and ship them",
          "To a rotating file inside the container",
          "Directly to the logging service over HTTP",
          "To syslog on the host"],
      c: 0,
      why: "A file inside a container disappears with the container and cannot be read from another node. Writing to stdout is the platform contract, and JSON means the collector can index the fields rather than regex-matching a sentence." },

    { q: "What is the practical purpose of a correlation id?",
      a: ["To tie every log line, job and downstream call from one request together",
          "To deduplicate identical log entries",
          "To identify the user across sessions",
          "To order log lines that share a timestamp"],
      c: 0,
      why: "Without it, debugging a report means guessing which of ten thousand interleaved lines belong together. Generated at the edge, propagated into queued jobs and outbound calls, and returned to the user, it turns a screenshot into a single log query." },

    { q: "Why report percentiles rather than the average response time?",
      a: ["The average hides the slow tail, and the p99 is the person actually complaining",
          "Percentiles are cheaper to compute",
          "Averages cannot be graphed over time",
          "Percentiles account for failed requests automatically"],
      c: 0,
      why: "A hundred fast requests and one ten-second request average out to something acceptable while one user waited ten seconds. Alerting on averages is how a team can be certain everything is fine while support is fielding calls." },

    { q: "What is the difference between a readiness check and a liveness check?",
      a: ["Readiness reports whether dependencies are usable; liveness reports only that the process is not wedged",
          "Readiness runs at startup and liveness runs continuously",
          "They are the same check exposed at two paths",
          "Liveness checks dependencies while readiness checks the process"],
      c: 0,
      why: "Conflating them is a real outage pattern: a liveness check that pings the database means a brief database blip makes the orchestrator kill every healthy container at once, turning a small problem into a total one." },

    { q: "What must never appear in a log line?",
      a: ["Credentials, tokens and personal data, which should be redacted at the logger",
          "Timestamps in local time",
          "Stack traces from handled exceptions",
          "The name of the class that logged it"],
      c: 0,
      why: "Logs are copied to aggregators, read by contractors and retained for years, so a logged token is a leaked token and logged personal data is a GDPR question. Redaction belongs in the logger itself, because relying on every call site is relying on nobody making a mistake." },

    { q: "What should be logged, as a default policy?",
      a: ["State changes, failures and decisions, rather than successful reads",
          "Every request and every query",
          "Only errors, to keep volume manageable",
          "Everything at debug level, filtered at collection"],
      c: 0,
      why: "Logging every read produces volume that costs money and buries the interesting lines. What you need months later is what changed, what failed, and which branch was taken. Reads are already visible in metrics." },

    { q: "What is wrong with logging routine, expected conditions at error level?",
      a: ["It trains everyone to ignore errors, so the real one is missed",
          "It consumes more storage than warning level",
          "Error level entries cannot carry a context array",
          "It causes the application to return a 500"],
      c: 0,
      why: "An alert that fires daily for something nobody acts on is an alert that has been switched off in everyone's head. Reserving error for things a human must look at is what keeps the signal meaningful." },

    { q: "What do the four golden signals cover?",
      a: ["Latency, traffic, errors and saturation",
          "CPU, memory, disk and network",
          "Requests, responses, retries and rollbacks",
          "Availability, durability, consistency and latency"],
      c: 0,
      why: "They are chosen to describe a service from the outside, in terms a user would recognise. Resource metrics tell you how the machine feels; the golden signals tell you whether the service is doing its job, which is the question that matters first." },

    { q: "What does distributed tracing add over structured logs with a correlation id?",
      a: ["Timing and parent-child relationships between spans, so you can see where the time went",
          "Automatic error grouping",
          "Retention of logs for longer periods",
          "The ability to log from multiple services"],
      c: 0,
      why: "Correlated logs tell you what happened and in what order. A trace tells you that the request spent 40 ms in your code and 900 ms waiting on one downstream call, which is the question you actually have when a page is slow." },
  ],

  /* ------------------------------------------------------ chapter 25b --- */
  "25b-production-support": [
    { q: "How should the severity of a production incident be determined?",
      a: ["By impact multiplied by scope, independently of how loudly it was reported",
          "By the seniority of the person reporting it",
          "By how long the fix is expected to take",
          "By how many separate complaints have arrived"],
      c: 0,
      why: "The loudest report is often a single frustrated user with a cosmetic problem, while a silent one affects every checkout. Assessing impact and scope first keeps triage honest and stops the queue being sorted by volume of email." },

    { q: "What is the first thing to do when an incident is reported, before any diagnosis?",
      a: ["Acknowledge it, so the reporter knows it is being handled",
          "Reproduce it in staging",
          "Check the most recent deploy",
          "Roll back to the previous release"],
      c: 0,
      why: "Silence causes escalation far more often than the bug itself does. A short acknowledgement with what you are doing and when you will next update buys the time you need to actually investigate, and it costs a minute." },

    { q: "What is the highest-value diagnostic question at the start of an incident?",
      a: ["What changed recently, including deploys, configuration and data imports",
          "Which server is affected",
          "How many users are affected",
          "Whether the error appears in the logs"],
      c: 0,
      why: "Systems that worked yesterday and not today usually changed. Deploys, feature flags, a certificate expiring, a partner's API version: asking what changed narrows the search before anyone opens an editor, and it is what experienced responders do first." },

    { q: "What is the correct order once a bug is understood?",
      a: ["Reproduce it, write a failing test, then fix it",
          "Fix it, then write a test if time permits",
          "Fix it in production, then reproduce locally",
          "Write the test after the fix is deployed"],
      c: 0,
      why: "A test written first proves you understood the bug and proves the fix works. Written afterwards it can only confirm the code you already wrote, and it is the step that gets skipped when the pressure lifts." },

    { q: "When choosing between a hotfix and a rollback, what should the default be?",
      a: ["Rollback, unless a migration has made it impossible",
          "Hotfix, since rolling back loses the other fixes in the release",
          "Whichever the on-call engineer is more comfortable with",
          "Hotfix, because rollbacks require a maintenance window"],
      c: 0,
      why: "Rollback returns to a known-good state and buys time to think, while a hotfix is untested code written under pressure. The exception matters: a destructive migration can make the previous release unable to run, which is the argument for expand and contract." },

    { q: "What does expand and contract mean for a schema change?",
      a: ["Add the new structure, migrate and run both, then remove the old one in a later release",
          "Expand the schema during deploy and contract it during rollback",
          "Add all columns as nullable and never drop any",
          "Run migrations in a transaction so they can be reversed"],
      c: 0,
      why: "Renaming a column in one step means the previous release cannot run against the new schema, so rollback is gone exactly when you need it. Splitting it across releases keeps both versions working simultaneously, which is what makes a safe rollback possible at all." },

    { q: "Why must a hotfix be branched from the production tag rather than from main?",
      a: ["main contains unreleased work that would be shipped alongside the fix",
          "Branching from main breaks the tag history",
          "Production tags contain compiled assets that main lacks",
          "The pipeline only builds from tags"],
      c: 0,
      why: "Branching from main means the urgent one-line fix drags in every merged feature since the last release, untested together and unreviewed for this deploy. Fix on the tag, deploy, then merge back so the fix is not lost." },

    { q: "What is the purpose of a blameless post-mortem?",
      a: ["To surface the systemic conditions that allowed the failure, which requires honesty about what happened",
          "To record who made the mistake for the performance review",
          "To satisfy a compliance requirement",
          "To assign the follow-up work to the responsible engineer"],
      c: 0,
      why: "If naming what you did gets you blamed, the accurate account never arrives and the same failure recurs. Blameless is not politeness, it is the precondition for learning anything, and the useful output is the missing guardrail rather than the person." },

    { q: "Which metric best indicates whether your monitoring is working?",
      a: ["Time to detect, especially whether you found it before a customer reported it",
          "Number of incidents per month",
          "Mean time to repair",
          "Number of alerts fired"],
      c: 0,
      why: "Repair time measures how good you are at fixing; detection time measures whether you would have known. An incident found by a customer email is a monitoring gap regardless of how quickly it was then fixed." },

    { q: "A customer-facing incident is resolved. What still remains to be done?",
      a: ["Tell the affected customers what happened and what prevents a recurrence",
          "Nothing, once the technical fix is deployed",
          "Close the ticket and record the downtime",
          "Schedule a retrospective and wait for it"],
      c: 0,
      why: "The technical half restores the service; the communication half restores the trust. A short, honest account of what broke and what changed as a result is remembered long after the outage, and its absence is remembered too." },
  ],

  /* ------------------------------------------------------- chapter 27 --- */
  "27-tooling-and-xdebug": [
    { q: "A breakpoint in PhpStorm never triggers, although Xdebug is installed and the page loads. What is the most likely cause?",
      a: ["Path mapping between the container and the editor is wrong, so the file identity does not match",
          "The Xdebug extension is not enabled",
          "The firewall is blocking port 9003",
          "The breakpoint is on a line the optimiser removed"],
      c: 0,
      why: "Xdebug reports the path as the server sees it, such as /var/www/html, and the IDE knows your local path. Without a mapping the debugger cannot tell it is the same file, so it silently never stops. It looks like a broken extension and almost never is." },

    { q: "Which two Xdebug features give the largest speed-up once step debugging works at all?",
      a: ["Conditional breakpoints and break-on-exception",
          "Profiling and code coverage",
          "Remote logging and stack traces",
          "Variable watches and the evaluation console"],
      c: 0,
      why: "Stepping through nine hundred iterations to reach the one that fails is why people give up on debuggers. A condition on the loop variable, and stopping automatically where an exception is thrown, take you straight to the interesting frame." },

    { q: "Why must Xdebug never be enabled on a production server?",
      a: ["It carries a large performance cost and its remote connection is a code-execution risk",
          "It writes logs that fill the disk",
          "It is incompatible with OPcache",
          "It changes the behaviour of exception handling"],
      c: 0,
      why: "Beyond the slowdown, an exposed debugger port allows an attacker to evaluate arbitrary code in your application's context. Xdebug belongs in the development image, and the production image should not contain the extension at all." },

    { q: "What is the practical value of introducing PHPStan with a baseline on an existing project?",
      a: ["Existing violations are frozen, so the tool passes today and blocks new debt from tomorrow",
          "It lowers the analysis level until the code passes",
          "It automatically fixes the reported issues",
          "It converts errors into IDE warnings only"],
      c: 0,
      why: "Ten thousand errors on day one means the tool gets disabled within a week. A baseline makes the check a ratchet: debt cannot grow, and it shrinks whenever a file is touched for other reasons, which is how legacy code gets better without a project to do it." },

    { q: "Why is automated code formatting worth adding even when the team already agrees on style?",
      a: ["It removes style from code review, so reviews are about design rather than spacing",
          "It reduces the size of the repository",
          "It is required by PSR-12 compliance tools",
          "It speeds up static analysis"],
      c: 0,
      why: "Every comment about a brace is a comment not spent on the logic, and it makes review feel adversarial for no benefit. A formatter run in CI settles it once, permanently, and nobody has to be the person who mentions it." },

    { q: "What should you do before optimising a slow endpoint?",
      a: ["Profile it, so the change targets where the time actually goes",
          "Add caching, since it always helps",
          "Rewrite the slowest-looking loop",
          "Increase the memory limit and measure again"],
      c: 0,
      why: "Intuition about performance is reliably wrong, and the fix that took an afternoon usually addressed three per cent of the time. Xdebug's profiler, Blackfire or a simple timing harness turns optimisation from guessing into arithmetic." },

    { q: "Why should CI grep for dd( and var_dump( in the codebase?",
      a: ["Left in production they dump connection details, environment values and tokens onto the page",
          "They cause fatal errors on PHP 8",
          "They prevent OPcache from caching the file",
          "They break static analysis"],
      c: 0,
      why: "It is not merely embarrassing. Dumping an object prints its properties, which for a database or client object includes credentials, and search engines index the result. A one-line CI check is cheap insurance against a bad afternoon." },

    { q: "What does Rector do that a formatter does not?",
      a: ["Applies semantic code transformations, such as upgrading syntax between PHP versions",
          "Enforces a consistent brace and indentation style",
          "Detects type errors without running the code",
          "Generates unit tests from existing code"],
      c: 0,
      why: "A formatter changes whitespace; Rector changes code, turning old constructs into new ones across thousands of files reliably. It is what makes a 5.6 to 8.2 migration a review exercise rather than a year of manual edits." },

    { q: "What is the difference between what PHPStan finds and what a test suite finds?",
      a: ["Static analysis reasons about all paths without running them; tests verify the paths they exercise",
          "Static analysis finds runtime errors; tests find type errors",
          "They find the same class of problem by different means",
          "Static analysis replaces the need for unit tests"],
      c: 0,
      why: "PHPStan will tell you a method can return null on a branch nobody wrote a test for, which no test can do unless somebody thought of that case. Tests tell you the behaviour is right, which no analyser can. They are complements, not alternatives." },

    { q: "Xdebug is installed and the application is now three times slower in development. What is the usual remedy?",
      a: ["Set the mode to debug only and trigger it per request, rather than leaving it always on",
          "Reduce the maximum nesting level",
          "Disable OPcache to compensate",
          "Increase the FPM worker count"],
      c: 0,
      why: "xdebug.mode covers debug, develop, coverage, profile and trace, and the expensive ones do not need to be on constantly. Combining a narrow mode with start_with_request set to trigger keeps normal development fast and the debugger one click away." },
  ],

  /* ------------------------------------------------------- chapter 28 --- */
  "28-containers-and-composer": [
    { q: "Under PSR-4, what determines where a class file must live?",
      a: ["The namespace maps to a directory prefix, and the file name matches the class name exactly",
          "Composer scans all directories and builds a map regardless of naming",
          "The file must be in the same directory as its interface",
          "Any location works, provided the file is listed in composer.json"],
      c: 0,
      why: "The rule is mechanical and it is case-sensitive on Linux, which is why a class that autoloads on a developer's Windows or macOS machine can fail in production. Filename equals class name is the habit that prevents that entire category of deploy surprise." },

    { q: "What is the difference between composer install and composer update?",
      a: ["install resolves nothing and reproduces the lock file exactly; update recalculates it",
          "install adds new packages; update only upgrades existing ones",
          "install works offline; update requires the network",
          "They are identical when a lock file exists"],
      c: 0,
      why: "That difference is the whole reason the lock file exists. Running update on a server means deploying versions nobody tested, at a time nobody chose. Update on your machine, review the diff, commit the lock, install everywhere else." },

    { q: "Which projects should commit composer.lock?",
      a: ["Applications, but not libraries",
          "Both applications and libraries",
          "Libraries, but not applications",
          "Neither, since the lock file is generated"],
      c: 0,
      why: "An application wants a reproducible, tested set of versions. A library is consumed by applications with their own constraints, and shipping a lock file there pins nothing for consumers while creating a file that is permanently out of date." },

    { q: "What does the caret constraint ^2.3 allow?",
      a: ["Any 2.x release from 2.3 upward, but not 3.0",
          "Only 2.3.x patch releases",
          "Any version 2.3 or higher, including 3.0",
          "Exactly 2.3"],
      c: 0,
      why: "It encodes the semver promise: minor and patch releases are meant to be compatible, major ones are not. Writing >=2.3 instead is an open invitation for a breaking 4.0 to arrive during an unrelated deploy." },

    { q: "Why should a Dockerfile copy composer.json and composer.lock before copying the application source?",
      a: ["So the dependency install layer is cached and only reruns when the manifests change",
          "Because Composer refuses to run if source files are present",
          "So the vendor directory is excluded from the final image",
          "Because layer order determines the file permissions"],
      c: 0,
      why: "Docker caches layers by their inputs. Copying everything first means every source edit invalidates the install layer and reinstalls the whole dependency tree. Manifests first is a one-line change that turns a two-minute rebuild into ten seconds." },

    { q: "What is the point of a multi-stage Docker build for a PHP application?",
      a: ["Build tools and dev dependencies stay out of the runtime image",
          "It allows the image to run on multiple architectures",
          "It compresses the layers into one",
          "It lets several services share one Dockerfile"],
      c: 0,
      why: "Composer, node, compilers and test fixtures are needed to produce the artefact and are pure attack surface at runtime. Copying only the built result into a slim final stage makes the image smaller to pull and smaller to exploit." },

    { q: "Why is deleting a secret in a later Docker layer not enough?",
      a: ["Earlier layers persist in the image and the file remains readable in them",
          "The delete only affects the container filesystem, not the image cache",
          "Docker restores deleted files at runtime",
          "The registry rejects images with deletions"],
      c: 0,
      why: "An image is a stack of layers and anyone who pulls it can read every one. Use build secrets or an argument that is never written to disk. This is a genuine leak vector for private repository tokens in Composer installs." },

    { q: "You need to upgrade one package without moving anything else. What is the correct command?",
      a: ["composer update vendor/package --with-dependencies",
          "composer update, then revert the unwanted changes in the lock file",
          "composer require vendor/package with an exact version",
          "composer install vendor/package"],
      c: 0,
      why: "Naming the package limits the resolution to it, and --with-dependencies allows the packages it needs to move too. Editing the lock file by hand produces a state the resolver never validated and will not reproduce." },

    { q: "What does composer dump-autoload --optimize do, and where does it belong?",
      a: ["Builds a static class map for faster autoloading, in the production build",
          "Reduces the size of the vendor directory, in development",
          "Regenerates composer.lock from composer.json, before deploying",
          "Validates PSR-4 compliance, in continuous integration"],
      c: 0,
      why: "Without it, each class triggers a filesystem check to find its file. The class map removes that per-class cost. It is skipped in development precisely because new files then require regenerating the map." },

    { q: "What should a containerised project's README need in order to be genuinely containerised?",
      a: ["Two commands: one to build or pull, one to run",
          "A list of required host extensions",
          "Instructions for installing PHP locally as a fallback",
          "A description of the image layers"],
      c: 0,
      why: "If a new developer needs a page of prerequisites and four manual steps, the container has not delivered its main benefit. Clone, one command, working environment: that is the standard the effort is supposed to buy." },
  ],

  /* ------------------------------------------------------- chapter 29 --- */
  "29-cloud-hosting": [
    { q: "Which single web-server setting matters most when deploying a modern PHP framework?",
      a: ["The document root must point at public/, not at the project directory",
          "The PHP version must match the development machine exactly",
          "The upload_max_filesize must exceed the largest expected request",
          "The server must run PHP-FPM rather than mod_php"],
      c: 0,
      why: "Point it at the project root and .env, composer.json, the vendor directory and the whole source tree become downloadable over HTTP. It is a single line of configuration and one of the most consequential in the entire deployment." },

    { q: "What does APP_DEBUG=true do on a production Laravel site when an error occurs?",
      a: ["Renders a debug page that can expose environment variables, including credentials",
          "Writes more detail to the log file only",
          "Disables caching so errors are reproducible",
          "Sends the stack trace to the configured error tracker"],
      c: 0,
      why: "The debug page prints the environment, and the environment holds the database password, the mail credentials and the application key. It is a very common finding on Italian shared hosting, and it takes one setting to prevent." },

    { q: "Why should env() never be called outside a configuration file in Laravel?",
      a: ["Once the configuration is cached, env() returns null in application code",
          "env() is slower than config()",
          "env() cannot read values containing spaces",
          "env() is deprecated in recent versions"],
      c: 0,
      why: "config:cache serialises the resolved configuration and the .env file is no longer read at runtime. Code calling env() directly then receives null, which usually manifests as a feature silently disabling itself in production only." },

    { q: "What is an atomic deploy, in the symlink sense?",
      a: ["The new release is built in its own directory and a symlink is repointed in one operation",
          "All servers are updated simultaneously",
          "The deploy runs inside a database transaction",
          "The old release is deleted before the new one is uploaded"],
      c: 0,
      why: "Uploading over a live directory means requests land midway through, mixing old and new files. Building beside it and switching a symlink means the change is instantaneous, and rolling back is repointing the symlink at the previous release." },

    { q: "Which directories must live outside the release directory in a symlink deployment?",
      a: ["Shared state such as uploaded files and the .env, symlinked into each release",
          "The vendor directory, so it is not rebuilt",
          "The public directory, so the web server does not follow the symlink",
          "The log directory only"],
      c: 0,
      why: "Anything written by the application must survive the switch. A user's uploaded PDF stored inside release 42 disappears when release 43 goes live, which is the classic first outing of an otherwise correct deploy script." },

    { q: "What must happen to PHP-FPM and to queue workers on every deploy?",
      a: ["FPM is reloaded and workers are restarted, or both keep running the old code",
          "Only FPM needs reloading; workers reload automatically",
          "Only workers need restarting; FPM detects file changes",
          "Neither, provided OPcache validate_timestamps is enabled"],
      c: 0,
      why: "OPcache with timestamp validation off keeps serving the old bytecode, and a worker process holds the old classes in memory indefinitely. Both are silent: the site looks deployed and behaves as it did before." },

    { q: "Why is .env.example committed while .env is not?",
      a: ["The example documents which variables exist without disclosing any values",
          "The example is used automatically when .env is missing",
          "Committing .env would break the lock file",
          "The example is required by Composer"],
      c: 0,
      why: "A new developer needs to know that MAIL_HOST exists; nobody needs the production password in Git history, where it lives forever even after being removed. Keeping the two files in step is a small discipline with a large downside when skipped." },

    { q: "What makes a backup an actual backup?",
      a: ["A restore has been tested, and the RPO and RTO are known",
          "It runs nightly and reports success",
          "It is stored on a different disk on the same server",
          "It is encrypted and retained for a year"],
      c: 0,
      why: "An untested backup is a file you hope is useful. Restoring it is the only way to learn that the dump excluded a table, or that a full restore takes six hours when the business assumed twenty minutes." },

    { q: "For a small Italian client's Laravel gestionale, what does a VPS give you over shared hosting?",
      a: ["Control of the document root, real cron and workers, and the PHP extensions you need",
          "Guaranteed uptime backed by a contract",
          "Lower cost at the same performance",
          "Automatic scaling under load"],
      c: 0,
      why: "The constraints that break a framework on shared hosting are structural: no control of the web root, no long-running processes, and a fixed extension set. Those are the arguments to make, in euros and limits rather than in taste." },

    { q: "A migration must run during a zero-downtime deploy. Which kind is safe?",
      a: ["Additive changes only, with destructive ones shipped in a later release",
          "Any migration, provided it runs inside a transaction",
          "Any migration, provided it runs before the symlink switch",
          "Only migrations that do not touch indexes"],
      c: 0,
      why: "During the switch, old and new code run against the same schema for a moment. Adding a nullable column is harmless to both; dropping one breaks the old release instantly and takes rollback away at the same time." },
  ],

  /* ------------------------------------------------------ chapter 29b --- */
  "29b-shared-hosting": [
    { q: "What is the first thing to request from a client's cPanel host before deploying a framework?",
      a: ["A subdomain or domain whose document root can point at public/",
          "SSH access with sudo",
          "An increase to the memory limit",
          "A dedicated IP address"],
      c: 0,
      why: "Almost every other constraint has a workaround; a document root you cannot move does not. If the panel allows the web root to be set per domain, the deployment becomes ordinary. If not, you are into rewrite tricks that leak source." },

    { q: "The document root cannot be changed. What is the least-bad arrangement?",
      a: ["Put the application above the web root and the contents of public/ inside it, adjusting the bootstrap paths",
          "Put everything in the web root and block access with .htaccess rules",
          "Rename public/ to public_html and leave the rest beside it",
          "Serve the application through a PHP proxy script"],
      c: 0,
      why: "Only the front controller and the assets belong in a public directory. Relying on .htaccess to hide the rest means one server misconfiguration or one unsupported directive away from serving your .env as plain text." },

    { q: "After deploying to shared hosting, what should you check yourself?",
      a: ["Request /.env, /composer.json and /.git/config over HTTP and confirm they are inaccessible",
          "That the site loads over HTTPS",
          "That the error log is writable",
          "That the PHP version matches development"],
      c: 0,
      why: "Automated scanners request those paths within hours of a domain appearing. Checking them yourself takes a minute and is the difference between finding the leak and having it found for you." },

    { q: "There is no supervisor and no persistent process. How do you run queued work?",
      a: ["A cron entry running the worker with a stop-when-empty flag, guarded by flock",
          "A long-running process started from a web request",
          "Processing jobs synchronously and increasing the timeout",
          "A JavaScript poller in the browser triggering a worker endpoint"],
      c: 0,
      why: "Cron plus stop-when-empty gives you a worker that starts, drains the queue and exits. flock prevents overlapping runs when one batch takes longer than the interval, which is the failure that produces duplicated work." },

    { q: "How should vendor/ reach a shared host with no Composer available?",
      a: ["Install locally and upload the built directory, ideally as one archive",
          "Upload composer.phar and run it over HTTP",
          "Commit vendor/ to Git and pull it on the server",
          "Upload the files one by one over FTP"],
      c: 0,
      why: "Uploading thousands of small files over FTP is slow and routinely leaves a partial tree that fails in a way nobody can diagnose. One archive extracted on the server is atomic enough to be reliable." },

    { q: "Why should the host's mail() function be avoided for transactional email?",
      a: ["Messages without proper SPF, DKIM and DMARC alignment land in spam",
          "mail() has been removed from PHP 8",
          "It cannot send HTML messages",
          "It is slower than SMTP"],
      c: 0,
      why: "A shared host's IP reputation is shared with everyone else on it, and a message that does not authenticate for your domain is filtered. Authenticated SMTP through a real provider is the fix, and the three DNS records are the actual work." },

    { q: "Why is leaving a .git directory in the web root dangerous?",
      a: ["The entire source history can be reconstructed from it by anyone",
          "It slows down every request",
          "It allows Git commands to be executed remotely",
          "It exposes only the branch names"],
      c: 0,
      why: "Downloading .git/config and the object files reconstructs the repository, including any credentials committed and later removed. Automated tools do this at scale, and deploying by pulling into the web root is how it usually happens." },

    { q: "PHP on the host runs as a user shared with other sites. What follows for file permissions?",
      a: ["World-readable files may be readable by other tenants, so permissions must be tightened",
          "All files must be 777 for the application to write",
          "Permissions are irrelevant because each account is chrooted",
          "Only the web root's permissions matter"],
      c: 0,
      why: "On older shared setups, a 644 .env in your account can be read by a script running under another. Restricting to the owner where the platform allows it, and keeping secrets out of the web tree entirely, is the mitigation available to you." },

    { q: "How should you argue for moving a client from shared hosting to a VPS?",
      a: ["With specific limits they are hitting and the euro cost of the workarounds",
          "By explaining that shared hosting is unprofessional",
          "By refusing to deploy until they upgrade",
          "By comparing the technical specifications of both"],
      c: 0,
      why: "The client chose shared hosting for a reason, usually price and inertia. Naming the constraint that costs them something concrete, and pricing the alternative, is a business conversation. Taste is not an argument anyone is obliged to accept." },

    { q: "Which framework feature most often fails silently on shared hosting?",
      a: ["Scheduled tasks and queued jobs, because there is no persistent process",
          "Database migrations, because of privilege restrictions",
          "Blade templating, because of open_basedir",
          "Session handling, because of the shared filesystem"],
      c: 0,
      why: "The site loads and looks correct while emails are never sent and nightly imports never run. Nothing errors, because nothing runs at all, which is why it is worth verifying the scheduler explicitly rather than assuming it works." },
  ],

  /* ------------------------------------------------------- chapter 30 --- */
  "30-ci-cd": [
    { q: "In what order should CI stages run, and why?",
      a: ["Cheapest and fastest first, so a lint failure does not wait behind an integration suite",
          "Alphabetically, so the pipeline is predictable",
          "Slowest first, so long stages start early",
          "In the order the tools were added to the project"],
      c: 0,
      why: "Fail fast is about feedback latency. A missing semicolon should be reported in twenty seconds, not after eleven minutes of browser tests. Lint, static analysis, unit, integration, deploy is the order that respects the developer waiting for it." },

    { q: "What does build once, deploy many mean?",
      a: ["One artefact is built, tested, and promoted unchanged through each environment",
          "One pipeline definition is shared by all projects",
          "The build cache is reused across branches",
          "The same commit is deployed to several servers simultaneously"],
      c: 0,
      why: "Rebuilding per environment means what you tested is not what you shipped: a dependency moved, a base image updated, a build flag differed. Promoting the identical artefact makes staging a genuine rehearsal rather than an approximation." },

    { q: "What is the correct cache key for the Composer dependency cache in CI?",
      a: ["A hash of composer.lock, so the cache is reused until dependencies actually change",
          "The branch name, so each branch keeps its own cache",
          "The commit SHA, for maximum accuracy",
          "A fixed string, so the cache is always reused"],
      c: 0,
      why: "The lock file is exactly what determines the contents of vendor/. Keying on the commit means a miss on every push, and a fixed key means a stale cache that silently keeps old packages around long after the lock changed." },

    { q: "Why do CI service containers need health checks?",
      a: ["Otherwise migrations run before the database is accepting connections and the job fails intermittently",
          "Otherwise the containers are not cleaned up after the job",
          "Otherwise they cannot be reached by hostname",
          "Otherwise their logs are not collected"],
      c: 0,
      why: "The container starts in a second and MySQL becomes ready several seconds later. Without waiting for readiness you get a flaky pipeline that fails perhaps one run in five, and flakiness trains the team to press retry rather than to read." },

    { q: "Why must deployment secrets be unavailable to pipelines triggered by a fork's pull request?",
      a: ["Anyone can open a pull request, and its code would run with your credentials",
          "Forks cannot decrypt the secrets anyway",
          "It would exceed the secret storage quota",
          "Secrets expire when used from a fork"],
      c: 0,
      why: "A pull request is untrusted code by definition. If the pipeline exposes deploy keys to it, a stranger can print them or push with them. Gate anything privileged on the branch and the repository, not merely on the pipeline succeeding." },

    { q: "Which migrations belong in the automated deploy step?",
      a: ["Additive ones only, with destructive changes deliberately deferred to a later release",
          "All pending migrations, so the schema always matches the code",
          "None, since migrations should always be manual",
          "Only those marked as reversible"],
      c: 0,
      why: "Additive changes leave the previous release able to run, which keeps rollback possible during the riskiest minutes of a deploy. A DROP COLUMN in the same step removes that option at the exact moment you might need it." },

    { q: "A test fails roughly one run in ten and passes on retry. What is the correct response?",
      a: ["Fix it the same day, or quarantine it, because a flaky gate stops being a gate",
          "Add an automatic retry so the pipeline goes green",
          "Increase the timeout and move on",
          "Disable it until someone has time"],
      c: 0,
      why: "Once retrying is normal, a genuine failure gets retried too and nobody looks at the output. The suite still runs but has stopped protecting anything, which is worse than not having it because the team believes it is covered." },

    { q: "Why should a rollback be rehearsed before it is needed?",
      a: ["The first attempt reveals the missing step, and an incident is the wrong time to find it",
          "Rollbacks require approval that takes time to obtain",
          "It warms the deployment cache",
          "It is required for compliance audits"],
      c: 0,
      why: "The shared directory nobody symlinked, the queue that needs restarting, the cached configuration pointing at the new release: these are discovered on the first real rollback. Discovering them during an outage doubles its length." },

    { q: "What distinguishes continuous delivery from continuous deployment?",
      a: ["Delivery keeps every build releasable with a human deciding when; deployment releases automatically",
          "Delivery covers testing while deployment covers building",
          "Delivery applies to libraries and deployment to applications",
          "They are two names for the same practice"],
      c: 0,
      why: "The distinction is where the button is. Delivery means any green commit could ship right now; deployment means it already did. Most Italian software houses practise delivery, often with an approval gate for the production environment." },

    { q: "What is the purpose of running the linter and static analysis in CI when everyone runs them locally?",
      a: ["Local runs are optional and CI is not, so the standard holds even under deadline pressure",
          "CI uses a newer version of the tools",
          "Local runs cannot access the full dependency tree",
          "It produces the coverage report"],
      c: 0,
      why: "Any check that depends on remembering will be skipped on the day it matters most. Moving it into the pipeline turns an agreement into a property of the repository, and removes the need for anyone to police it in review." },
  ],

  /* ------------------------------------------------------- chapter 31 --- */
  "31-wordpress": [
    { q: "What is the difference between a WordPress action and a filter?",
      a: ["A filter receives a value and must return it; an action performs work and returns nothing",
          "Actions run before the page renders and filters run after",
          "Filters are for admin pages and actions for the front end",
          "Actions can be removed and filters cannot"],
      c: 0,
      why: "Forgetting to return from a filter callback wipes the value: a title becomes empty, a price becomes null. It is the most common WordPress mistake there is, and it produces a blank where content should be rather than an error." },

    { q: "A client needs a change to a third-party plugin's behaviour. What is the professional approach?",
      a: ["Use its hooks from your own plugin, or fork it deliberately, but never edit it in place",
          "Edit the plugin file and document the change in a README",
          "Copy the plugin into the theme directory and edit it there",
          "Disable automatic updates and edit freely"],
      c: 0,
      why: "An in-place edit is erased by the next update, silently, usually months later when nobody connects the two events. Hooks survive updates, and a deliberate fork at least makes the divergence visible in version control." },

    { q: "When should data be a taxonomy rather than post meta?",
      a: ["When it is used to filter or group posts, because meta queries do not scale",
          "When it is a string rather than a number",
          "When it must be visible in the admin interface",
          "When more than one value is possible"],
      c: 0,
      why: "Taxonomy terms have their own indexed relationship tables. A meta_query filters by joining and scanning a key-value table, so a catalogue filtered by three meta fields on ten thousand posts becomes visibly slow and no index will rescue it." },

    { q: "How must a direct database query be written in WordPress?",
      a: ["Through the wpdb prepare method with placeholders, never by concatenating values",
          "With mysqli_real_escape_string applied to each value",
          "Through the query method after casting inputs to their expected types",
          "Using PDO with a separate connection"],
      c: 0,
      why: "prepare is WordPress's parameterised query interface and it is the only supported way to get values into SQL safely. Escaping by hand fails the same way it fails everywhere else, and the plugin repository rejects code that does it." },

    { q: "What do a nonce and a capability check each prove, and why are both needed?",
      a: ["The nonce proves the request came from your form; the capability proves the user is allowed",
          "The nonce proves identity; the capability proves the session is current",
          "They are redundant, and the capability check alone is sufficient",
          "The nonce prevents replay; the capability prevents SQL injection"],
      c: 0,
      why: "A nonce stops a third-party site making the request on the user's behalf, which is CSRF. It says nothing about permission: an editor's valid nonce does not entitle them to delete users. Two checks, two distinct questions." },

    { q: "Which escaping function belongs inside an HTML attribute?",
      a: ["esc_attr, chosen because escaping is context-dependent",
          "esc_html, which is safe in every context",
          "sanitize_text_field, applied when the value is saved",
          "wp_kses, which allows a list of safe tags"],
      c: 0,
      why: "Escaping late and per context is the WordPress rule: esc_html in text, esc_attr in attributes, esc_url in URLs, esc_js in script. Using the wrong one either breaks the output or leaves an injection point the right one would have closed." },

    { q: "Why must a database search-and-replace during a WordPress migration use a tool that understands serialisation?",
      a: ["Serialised PHP strings carry length prefixes, so a raw SQL replace corrupts them",
          "The database uses a proprietary encoding for options",
          "URLs are stored hashed and must be recomputed",
          "Direct replacement bypasses the object cache"],
      c: 0,
      why: "A serialised value records the byte length of each string, and changing the text without updating the number makes the whole option unreadable. Widgets and settings vanish. WP-CLI's search-replace unserialises and reserialises correctly." },

    { q: "Why should wp-cron be replaced with a real cron entry on any site that matters?",
      a: ["wp-cron runs only when someone visits, so scheduled tasks are late on quiet sites and add load on busy ones",
          "wp-cron cannot run more than once per hour",
          "wp-cron is disabled by default in recent versions",
          "wp-cron requires an active admin session"],
      c: 0,
      why: "It is not a scheduler, it is a check performed on page load. A low-traffic site's nightly job may run at noon the following day, and a high-traffic site pays for the check on every request. A system cron entry plus disabling the built-in one fixes both." },

    { q: "What is the correct way to add custom PHP to a client's WordPress site?",
      a: ["A site-specific plugin, so the code survives a theme change",
          "The theme's functions.php file",
          "A must-use plugin, which cannot be deactivated",
          "The wp-config.php file"],
      c: 0,
      why: "Functionality in functions.php disappears when the theme is switched, which a client or a designer may do without warning. A small plugin keeps behaviour separate from presentation, which is the same separation you would insist on anywhere else." },

    { q: "What is the practical significance of WordPress running a large share of Italian small-business sites?",
      a: ["Agency work in this market frequently means maintaining and extending it professionally",
          "It means WordPress skills are unnecessary for developer roles",
          "It means all such sites use the same theme",
          "It means PHP experience alone is sufficient"],
      c: 0,
      why: "Adverts from web agencies in Bologna and Modena list it alongside Laravel for a reason. Treating it as beneath a real developer is a poor read of the market, and doing it well, with hooks, version control and proper escaping, is a genuine differentiator." },
  ],

  /* ------------------------------------------------------- chapter 32 --- */
  "32-ecommerce": [
    { q: "How should a price be stored in an e-commerce database?",
      a: ["As an integer number of cents, or as DECIMAL, never as a float",
          "As a float, since prices have decimal places",
          "As a formatted string in the shop's locale",
          "As a double for precision"],
      c: 0,
      why: "Binary floating point cannot represent 0.10 exactly, so sums drift. One cent of drift reaches the accountant, then the client, and the trust cost far exceeds the effort of using integers. This is the single most repeated rule in the money chapters." },

    { q: "How should VAT be calculated on a cart with several lines at different rates?",
      a: ["Per line, rounded per line, then summed",
          "On the cart total, using the highest applicable rate",
          "On the cart total, using a weighted average rate",
          "Per line without rounding, rounding only the final total"],
      c: 0,
      why: "Italian carts routinely mix 22, 10 and 4 per cent, and the accepted practice is to compute and round each line then add them. Rounding only at the end produces totals that disagree with the invoice by a cent, which an accountant will notice." },

    { q: "Why must the decision between net and gross pricing be made once and written down?",
      a: ["Mixing the two conventions produces totals that are wrong in ways that are very hard to trace",
          "The database schema cannot represent both",
          "Tax law requires gross prices in all cases",
          "Payment gateways accept only one of the two"],
      c: 0,
      why: "One module storing prices excluding VAT and another including it means every calculation crossing that line is silently off by the tax rate. It is not caught by tests written under one assumption, and it usually surfaces in a reconciliation months later." },

    { q: "Why must the price and VAT rate be copied onto the order line at purchase time?",
      a: ["The order is a historical fact and must not change when the catalogue price changes",
          "It avoids a join when displaying the order",
          "The product row may be deleted",
          "It allows the price to be edited per order"],
      c: 0,
      why: "Re-deriving an old order from today's catalogue rewrites history: last year's invoice suddenly shows this year's price and no longer matches what the customer paid or what was declared. Snapshot the facts that must stay true." },

    { q: "Two customers buy the last unit simultaneously. What prevents overselling?",
      a: ["A conditional UPDATE that decrements only if enough stock remains, checking the affected row count",
          "Reading the stock level and then updating it in the same request",
          "A unique index on the product id",
          "Setting the transaction isolation level to READ COMMITTED"],
      c: 0,
      why: "Check-then-act has a window between the read and the write where both requests see one unit available. Making the database perform the check inside the UPDATE, and treating zero affected rows as failure, closes the window without a lock held across think-time." },

    { q: "What makes a checkout idempotent?",
      a: ["A client-supplied key with a unique constraint, so a repeated submission returns the original order",
          "Disabling the submit button after the first click",
          "Wrapping the checkout in a database transaction",
          "Using a POST-redirect-GET pattern"],
      c: 0,
      why: "The button guard fails on a double tap, a browser retry or a flaky connection, and none of those are rare. A unique key enforced by the database is the only mechanism that survives all of them, and it turns a duplicate into a no-op." },

    { q: "The browser returns from the payment provider with a success page, but the webhook has not arrived. What is the truth about the payment?",
      a: ["The webhook is the authoritative signal; the redirect can be faked or lost",
          "The redirect is authoritative because the user completed the flow",
          "Whichever arrives first should be trusted",
          "Both must agree before the order is confirmed"],
      c: 0,
      why: "A redirect is a URL in a browser the customer controls, and it may never arrive if they close the tab. The server-to-server webhook, signature verified, is the fact. Confirming orders on the redirect is a well-known way to ship goods for free." },

    { q: "Why should card details never touch your server, even briefly?",
      a: ["It brings the whole application into PCI DSS scope, at a cost no small project can carry",
          "PHP cannot encrypt card numbers securely",
          "Card networks technically block non-certified servers",
          "It is impossible to validate a card number server-side"],
      c: 0,
      why: "Hosted fields or a redirect keep the data with the provider, and your scope shrinks to a much smaller questionnaire. Handling the number yourself means audits, network segmentation and liability that a client's shop will not fund." },

    { q: "What is the main cost of choosing Magento for a small catalogue?",
      a: ["Operational and development weight far beyond what the requirement needs",
          "Its licence fee",
          "Its lack of Italian VAT support",
          "Its inability to handle product variants"],
      c: 0,
      why: "Magento is powerful and expensive to run and to staff. For a few hundred products, WooCommerce or PrestaShop, or a bespoke Laravel shop, will be cheaper to build and to keep alive. Choosing the platform on scale is the judgement being assessed." },

    { q: "Which product-catalogue feature most often causes performance problems as a shop grows?",
      a: ["Filtering on attributes stored as key-value rows rather than indexed columns",
          "Storing product images in the database",
          "Using UUIDs for product identifiers",
          "Having more than one currency"],
      c: 0,
      why: "The entity-attribute-value model is flexible and hostile to indexing: each filter is another join into a large table. It is why faceted search in a serious catalogue ends up in a search engine rather than in SQL." },
  ],

  /* ------------------------------------------------------- chapter 33 --- */
  "33-gestionali-and-sdi": [
    { q: "What is a gestionale, in the Italian software market?",
      a: ["Business management software covering orders, invoicing, stock and accounting for a company's daily operations",
          "A content management system for corporate websites",
          "A project management tool used by development teams",
          "A reporting layer built on top of an ERP"],
      c: 0,
      why: "It is the software a company actually runs on, and a very large share of the work in Emilia-Romagna and Lombardy is building, extending or integrating one. The domain is unglamorous and full of real invariants, which makes it good engineering ground." },

    { q: "How does an electronic invoice reach its recipient in Italy?",
      a: ["It is sent to the Sistema di Interscambio, which validates it and forwards it, replying asynchronously",
          "It is emailed directly to the customer as a signed PDF",
          "It is uploaded to the customer's own portal",
          "It is registered with the Chamber of Commerce"],
      c: 0,
      why: "The SDI sits between every seller and buyer, and its answer arrives later, by a separate notification. That asynchrony is why an invoice is a state machine rather than a boolean, and why a naive implementation that assumes success is wrong from day one." },

    { q: "Which SDI notifications must an application handle explicitly?",
      a: ["Rejection, failed delivery and non-delivery, each with a different consequence",
          "Only rejection, since the others are informational",
          "Only successful delivery, since failures are retried automatically",
          "None, because the intermediary resolves them"],
      c: 0,
      why: "A rejection means the invoice is legally not issued and must be corrected and resent within a deadline. Failed or non-delivery means it is valid but the recipient must fetch it. Collapsing these into a single 'sent' flag guarantees a compliance problem." },

    { q: "How must invoice numbers be allocated?",
      a: ["Sequentially with no gaps, reserved inside the transaction rather than computed as MAX plus one",
          "As UUIDs, to guarantee uniqueness",
          "Sequentially, computed as MAX plus one at insert time",
          "By the SDI, which assigns them on acceptance"],
      c: 0,
      why: "MAX plus one has a race that two concurrent invoices will find, producing a duplicate or a gap, and both are findings in an audit. A reserved counter row updated within the same transaction is the standard answer, and the requirement is legal rather than aesthetic." },

    { q: "An invoice has been accepted by the SDI and contains an error. What is the correct remedy?",
      a: ["Issue a nota di credito, because an accepted invoice is immutable",
          "Edit the invoice and resend it with the same number",
          "Delete the invoice and issue a replacement",
          "Send a corrected version with a suffix on the number"],
      c: 0,
      why: "Once accepted the document exists in law and cannot be changed or withdrawn. The credit note is the mechanism for correction. A system whose UI offers an edit button on an accepted invoice has misunderstood the domain at a fundamental level." },

    { q: "Which values are valid for the CodiceDestinatario field?",
      a: ["A seven-character code, a six-character one for public administration, or seven zeros with a PEC address",
          "Any string up to fourteen characters",
          "The recipient's VAT number",
          "The recipient's PEC address only"],
      c: 0,
      why: "Getting it wrong produces a rejection or a failed delivery days later, long after the user considered the job done. Validating it at entry, with the seven-zeros plus PEC case handled, prevents a support conversation that is otherwise guaranteed." },

    { q: "Why must codes such as CAP, partita IVA and codice destinatario be stored as strings?",
      a: ["Leading zeros are meaningful and an integer column destroys them",
          "They may exceed the maximum integer size",
          "String comparison is faster for these lengths",
          "They can contain letters in some regions"],
      c: 0,
      why: "A CAP of 00185 becomes 185 in an integer column, and nobody notices until an address fails validation or a label prints wrong. The rule generalises: if you never do arithmetic on it, it is an identifier, and identifiers are strings." },

    { q: "You are synchronising customer records with an ERP you cannot change. What is the essential design decision?",
      a: ["Deciding, per field, which system is the owner",
          "Choosing between REST and SOAP for the transport",
          "Deciding how often the synchronisation runs",
          "Whether to synchronise in batches or per record"],
      c: 0,
      why: "Two-way synchronisation on the same field means the last writer wins and neither system is right. Writing down the owner of each field turns an unbounded conflict problem into a set of one-directional copies, which is a thing you can actually test." },

    { q: "An import file arrives without a record that existed yesterday. What should the system do?",
      a: ["Nothing destructive, because absence usually means a partial or filtered export",
          "Delete the corresponding record, since the source is authoritative",
          "Mark the record inactive automatically",
          "Halt the import and alert an operator"],
      c: 0,
      why: "Treating absence as deletion means one truncated file wipes real data. Deletions should be explicit, and if the format cannot express them, the safe reading of a missing row is 'not in this file', not 'no longer exists'." },

    { q: "Why is an invoice's state best modelled explicitly rather than as a sent flag?",
      a: ["It moves through draft, sent, accepted, rejected and delivered, each with different permitted actions",
          "Because the database cannot index boolean columns",
          "Because the SDI requires a status field in the XML",
          "Because the UI needs to display a progress bar"],
      c: 0,
      why: "What a user may do depends on where the document is: a draft is editable, a sent one is not, a rejected one must be corrected and resent, an accepted one can only be credited. A boolean cannot express that, so the rules end up scattered across the code." },
  ],

  /* ------------------------------------------------------ chapter 33b --- */
  "33b-ai-in-php": [
    { q: "Why should a call to a language model API not happen inside a web request?",
      a: ["It holds an FPM worker for seconds, and concurrency is bounded by the worker count",
          "PHP cannot make HTTPS requests to those endpoints",
          "The response is too large for PHP's memory limit",
          "API keys cannot be read during a web request"],
      c: 0,
      why: "Ten seconds per call with twenty workers means two requests per second before the site stops responding entirely. Queue the call and deliver the result by polling, a websocket or an email, which is the same reasoning as any slow outbound integration." },

    { q: "What should be logged from the first day of an AI feature?",
      a: ["Token counts and cost per call, attributed to a feature and a user",
          "The full prompt and response for every call",
          "Only the failures, to keep volume down",
          "The model version only"],
      c: 0,
      why: "Cost you do not measure is cost you cannot forecast or attribute, and the first invoice is where an unbudgeted feature becomes a difficult conversation. Note that logging full prompts may itself store personal data, so log the counts and sample carefully." },

    { q: "A user's text is included in a prompt. What is the correct security assumption?",
      a: ["It is untrusted input for which no escaping exists, so authorisation must live outside the prompt",
          "It is safe once quotes and newlines are escaped",
          "It is safe if the system prompt instructs the model to ignore instructions",
          "It is safe when the model is given a low temperature"],
      c: 0,
      why: "There is no parameterised query for natural language: instructions and data share one channel. Telling the model to refuse is mitigation, not a control. Whatever the model returns must be checked by your code before it is allowed to do anything." },

    { q: "A model returns which record to display. What must your code do?",
      a: ["Check the current user's permission for that record before returning it",
          "Trust the model, since the prompt contained only permitted records",
          "Log the access and return it",
          "Re-ask the model to confirm the permission"],
      c: 0,
      why: "Authorisation is a property of your system, not an instruction the model can be trusted to follow. Prompt injection or an ordinary hallucination both produce the same outcome: an id your code fetched without asking whether this user may see it." },

    { q: "Model output is rendered on a page. What must happen first?",
      a: ["Escape it, because it is a string from an untrusted source like any other",
          "Nothing, because model output contains no markup",
          "Strip all HTML tags with strip_tags",
          "Store it in the database first, which sanitises it"],
      c: 0,
      why: "It is text produced from inputs an attacker may influence, so it can contain a script tag. Rendering it raw is stored cross-site scripting with an unusual origin, and the escaping rules are exactly the ones from the security chapter." },

    { q: "What is an embedding, in the context of retrieval-augmented generation?",
      a: ["A numeric vector representing a piece of text, so similar meanings are near each other",
          "A compressed copy of the document stored for retrieval",
          "A hash used to deduplicate documents",
          "The model's internal representation of the prompt"],
      c: 0,
      why: "Similarity search over vectors is how you find the passages worth putting in the prompt. In PHP this usually means a vector column or extension in Postgres, or a dedicated store, plus a pipeline that keeps the embeddings in step with the source." },

    { q: "Why should a model never be asked to apply a business rule or perform arithmetic?",
      a: ["It generates plausible text rather than computing, so it will be confidently wrong sometimes",
          "It is slower than doing it in PHP",
          "The API charges extra for numeric output",
          "Results cannot be formatted to two decimal places"],
      c: 0,
      why: "A VAT total that is right ninety-nine times and wrong once is worse than useless, because nobody checks the hundredth. Deterministic rules belong in code that can be tested; the model is for the parts where language is the problem." },

    { q: "How should model output be validated when it feeds a structured process?",
      a: ["Against a schema and an allowlist of permitted values, rejecting anything outside it",
          "By asking the model to check its own answer",
          "By comparing two responses at different temperatures",
          "By logging it and correcting problems as they are reported"],
      c: 0,
      why: "It is a text generator, and even with structured output modes the guarantee is not absolute. Treating the response the way you would treat a request body from the internet, validated at the boundary into a typed object, is the same discipline you already have." },

    { q: "Sending customer data to a model provider raises which obligation?",
      a: ["It is a data transfer to a processor and needs a legal basis and possibly a DPA",
          "None, since the data is not stored by the provider",
          "Only an update to the privacy policy",
          "Only an obligation if the provider is outside the EU"],
      c: 0,
      why: "Under the GDPR this is processing by a third party, and where the provider sits determines the additional safeguards. The engineering point is that this is a question to ask before building, not a compliance detail to reconcile afterwards." },

    { q: "Where does an AI feature fit best in a typical gestionale?",
      a: ["Drafting, summarising and classifying text, with a human confirming before anything is committed",
          "Calculating invoice totals to reduce code",
          "Deciding which orders to approve automatically",
          "Replacing the search index"],
      c: 0,
      why: "The good fits are where language is genuinely the problem and a human remains in the loop: summarising a ticket thread, drafting a reply, suggesting a category. The bad fits are the places where being wrong is expensive and nobody would notice." },
  ],

  /* ------------------------------------------------------- chapter 34 --- */
  "34-agile": [
    { q: "What is the daily stand-up actually for?",
      a: ["Surfacing blockers and coordinating, not reporting status to a manager",
          "Giving the team lead a progress update",
          "Assigning the day's tasks",
          "Reviewing the previous day's commits"],
      c: 0,
      why: "A meeting where each person narrates yesterday to a manager is a status report with extra steps, and it is why so many people find stand-ups pointless. The value is in the sentence 'I am stuck on X', said early enough to matter." },

    { q: "Which ceremony most deserves protection when the calendar is full?",
      a: ["Backlog refinement, because it is where vague requests become work that can be started",
          "The retrospective, because morale matters most",
          "The daily stand-up, because it is shortest",
          "The sprint review, because stakeholders attend"],
      c: 0,
      why: "Skipping refinement means the sprint starts with tickets nobody understands, and the cost appears as mid-sprint discovery, rework and missed acceptance criteria. It is the meeting that prevents the problems the other meetings then discuss." },

    { q: "What do story points measure?",
      a: ["Relative size and uncertainty, not hours",
          "The number of hours the work will take",
          "The business value of the item",
          "The priority relative to other items"],
      c: 0,
      why: "The moment points are converted to hours they become a commitment and then a target, and estimates start being negotiated rather than given. Points work because they are deliberately not a promise about a clock." },

    { q: "You are asked how long a task will take and you genuinely do not know. What is the best answer?",
      a: ["A range with the specific uncertainty named, plus what would narrow it",
          "The optimistic figure, since padding is dishonest",
          "The pessimistic figure, to be safe",
          "A refusal to estimate until the work is fully specified"],
      c: 0,
      why: "'Two to five days, depending on whether their API supports partial updates; I can tell you tomorrow' gives a planner something to work with and tells them where the risk sits. A single confident number hides both." },

    { q: "You realise on Tuesday that Friday's deadline will slip. When should you say so?",
      a: ["Immediately, because the value of the information decays every day",
          "Thursday, once you are certain",
          "Friday, along with what was completed",
          "At the retrospective, so it can be discussed properly"],
      c: 0,
      why: "On Tuesday somebody can descope, reassign or warn the client. On Friday they can only apologise. Raising a slip early is uncomfortable once and is the single highest-trust habit an engineer can have." },

    { q: "What is wrong with silently padding an estimate?",
      a: ["The buffer belongs to the plan, and hiding it corrupts everyone's model of how long work takes",
          "It is a form of dishonesty that violates the Scrum guide",
          "Padded estimates cannot be tracked in velocity",
          "It causes the work to expand to fill the time"],
      c: 0,
      why: "If everyone pads, planning is done with numbers nobody believes, and the response is to cut them arbitrarily, which encourages more padding. Naming the risk instead lets the team hold the buffer where it can be managed." },

    { q: "What makes an acceptance criterion useful?",
      a: ["It is testable and agreed before the work starts",
          "It is written by the developer who will implement it",
          "It describes the technical approach",
          "It is detailed enough to remove all ambiguity"],
      c: 0,
      why: "'The user should have a good experience' cannot be verified, so done becomes a matter of opinion at the worst possible moment. Testable criteria settled in advance are what make 'is it finished' a question with an answer." },

    { q: "Why do work-in-progress limits improve throughput?",
      a: ["Finishing before starting reduces context switching and gets value delivered sooner",
          "They force developers to work faster",
          "They reduce the number of meetings required",
          "They make velocity easier to calculate"],
      c: 0,
      why: "Five items at eighty per cent deliver nothing. Context switching is expensive and mostly invisible, and a board full of started work hides the fact that nothing has actually shipped. Finishing is the constraint worth optimising." },

    { q: "A company describes itself as agile but has a fixed six-month roadmap and no retrospectives. How should you handle it in an interview?",
      a: ["Ask concretely how they plan and adapt, without correcting their vocabulary",
          "Point out that this is not agile according to the manifesto",
          "Say nothing and accept the description",
          "Ask why they have not adopted Scrum properly"],
      c: 0,
      why: "Most organisations run something partial and know it. Asking how work arrives and how change is handled tells you what daily life is like, which is the thing you actually need to know. Correcting their terminology costs the room and teaches you nothing." },

    { q: "What is the purpose of a definition of done?",
      a: ["A shared, explicit standard so finished means the same thing to everyone",
          "A checklist for the tester to complete",
          "The acceptance criteria for a specific ticket",
          "A record of what was delivered in the sprint"],
      c: 0,
      why: "Without it, one person's done is merged and another's is deployed with documentation and monitoring. Writing it down once ends a recurring argument and makes the hidden work, tests and review and deployment, visible in planning." },
  ],

  /* ------------------------------------------------------- chapter 35 --- */
  "35-teamwork": [
    { q: "A stakeholder asks for 'a button that exports to Excel'. What should you establish first?",
      a: ["What they are trying to achieve, since the request arrived already shaped as a solution",
          "Which Excel version they use",
          "How many rows the export must support",
          "Whether the export should be synchronous or queued"],
      c: 0,
      why: "Requests arrive as solutions because people are being helpful. The underlying need might be a monthly figure for a meeting, which a saved report answers better. Asking about the goal costs one question and occasionally saves a week." },

    { q: "Why is the manual workaround a user already performs so valuable?",
      a: ["It is the specification, including the exceptions nobody would have thought to mention",
          "It shows how slow the current process is",
          "It proves the feature is technically possible",
          "It identifies who the primary user will be"],
      c: 0,
      why: "The spreadsheet somebody maintains by hand encodes the real rules, including the special case for one supplier that nobody documented. Watching it once tells you more than three meetings, and the exceptions are where projects overrun." },

    { q: "You have understood a vague request. What is the highest-value next step?",
      a: ["Write your understanding back to them in a few lines and get confirmation before building",
          "Start with a prototype so they have something to react to",
          "Break it into tickets and estimate them",
          "Ask a further round of clarifying questions"],
      c: 0,
      why: "Five minutes of written summary catches the mismatch while it costs nothing. It also creates a record both sides agreed to, which is quietly useful when the requirement is remembered differently a month later." },

    { q: "You are stuck. What does good practice look like before asking for help?",
      a: ["Timebox the attempt, then ask with what you tried, what you observed and what you think",
          "Keep trying until you solve it, since asking signals weakness",
          "Ask immediately, since somebody else's time is cheaper than yours",
          "Post the error message and wait"],
      c: 0,
      why: "Autonomy is not silence and it is not stubbornness. A bounded attempt respects your time; a well-formed question respects theirs, and often gets a better answer because you have already eliminated the obvious." },

    { q: "You solve a non-obvious problem after a long investigation. What should you do with the answer?",
      a: ["Write it where the next person will look, such as the repository or the ticket",
          "Remember it for next time",
          "Tell the colleague most likely to hit it",
          "Add a comment in the code only"],
      c: 0,
      why: "The next person is often you, in eight months, with no memory of the reasoning. Writing it down is the cheapest possible moment to do so, and a README or an architecture note outlives every chat message about it." },

    { q: "What distinguishes a review comment that helps from one that irritates?",
      a: ["It addresses the code rather than the person and states how much it matters",
          "It offers the corrected code directly",
          "It is phrased as a question rather than a statement",
          "It is delivered privately rather than in the pull request"],
      c: 0,
      why: "'This will break with a null customer' is actionable; 'you always forget null checks' is about the person. Labelling severity, whether it blocks or is merely a preference, saves the author guessing which comments they must act on." },

    { q: "Why do small pull requests receive better reviews?",
      a: ["A large diff is skimmed, so the review approves rather than examines",
          "Small pull requests can be merged without approval",
            "Reviewers are assigned by diff size",
          "Large diffs cannot be tested automatically"],
      c: 0,
      why: "Attention does not scale with line count. Eight hundred lines get a rubber stamp and a comment about naming. Splitting into reviewable pieces is a favour to the reviewer and gets you the scrutiny you actually wanted." },

    { q: "How should you describe a technically infeasible-sounding request to a non-technical stakeholder?",
      a: ["As a cost and a trade-off, with an alternative, rather than as impossible",
          "As impossible, clearly, so expectations are set",
          "By explaining the technical obstacles in detail",
          "By deferring to a more senior colleague"],
      c: 0,
      why: "'Impossible' is almost always shorthand for 'expensive', and stakeholders correctly hear it as a refusal. 'That is about three weeks because of X; this alternative is two days and covers most of it' hands them a decision they are entitled to make." },

    { q: "What should you do about a part of your own pull request you are unsure of?",
      a: ["Say so explicitly in the description, pointing the reviewer at it",
          "Remove it and open a separate pull request",
          "Leave it, since the reviewer's job is to find issues",
          "Add a TODO comment in the code"],
      c: 0,
      why: "Reviewers have limited attention and spend it where you direct it. Flagging the uncertain part gets it examined properly, and it signals judgement rather than doubt. Silence there is how a known-shaky change gets approved." },

    { q: "You disagree with a review comment. What is the productive response?",
      a: ["State your reasoning and ask for theirs, treating it as a question about the code",
          "Make the change to avoid friction",
          "Explain why the reviewer has misunderstood the design",
          "Escalate to the team lead for a decision"],
      c: 0,
      why: "Reviews improve code by exposing reasoning, and quietly complying wastes the disagreement's information. It is a technical conversation with a right answer often available; making it about who is correct is what turns review into a chore." },
  ],

  /* ------------------------------------------------------- chapter 36 --- */
  "36-cv-and-linkedin": [
    { q: "What is the structure of a CV bullet that actually communicates?",
      a: ["What you did, with what, and what changed as a result",
          "The technology, the project name, and the duration",
          "The responsibility, the team size, and the reporting line",
          "The problem, the analysis, and the proposal"],
      c: 0,
      why: "'Responsible for the backend' says nothing that distinguishes you from anyone else who held the role. 'Rebuilt the order import in Laravel, cutting a nightly job from four hours to twenty minutes' is a specific claim they can ask about." },

    { q: "Which formatting choices defeat an applicant tracking system?",
      a: ["Multi-column layouts, text inside images, and content in headers or tables",
          "Serif fonts and coloured section headings",
          "A photograph and a personal summary",
          "Reverse-chronological ordering"],
      c: 0,
      why: "Parsers read a linear text stream. A two-column design interleaves the columns into nonsense, and a header is often skipped entirely, which is how a candidate's contact details go missing. One column with selectable text is the safe shape." },

    { q: "What does the GDPR line on an Italian CV do?",
      a: ["Authorises the processing of your personal data, and some recruiters filter on its presence",
          "Prevents the recruiter from sharing your CV with clients",
          "Is legally required before you may apply",
          "Requests deletion of your data after the selection"],
      c: 0,
      why: "It is one line, it is conventional in this market, and its absence occasionally gets a CV discarded by a process rather than a person. There is no upside to omitting it." },

    { q: "A junior CV runs to two pages. What is the correct edit?",
      a: ["Cut the oldest and least relevant material to reach one page",
          "Reduce the font size and margins to fit",
          "Keep both pages, since more detail helps",
          "Move the older content into a linked portfolio"],
      c: 0,
      why: "The first pass takes seconds and the second page is rarely reached. Cutting the oldest keeps the strongest material where it will be seen, and the discipline of choosing is itself visible to the reader." },

    { q: "Why does the city belong on a CV in this market?",
      a: ["Employers filter on commuting distance, and its absence is read as being far away",
          "It is legally required on Italian CVs",
          "It helps the ATS categorise the application",
          "It establishes tax residency for the contract"],
      c: 0,
      why: "A Modena software house shortlisting for an on-site role sorts by who can be there on Monday. Omitting the city to seem more available achieves the opposite, because the reader assumes the worst rather than asking." },

    { q: "How should language ability be stated?",
      a: ["With CEFR levels such as B2 or C1, which are testable and comparable",
          "As buono, ottimo or madrelingua",
          "By naming certificates and their dates only",
          "As a self-assessed percentage"],
      c: 0,
      why: "'Buono' means whatever the reader assumes and cannot be checked, and it is often the point where a screening call goes wrong. A CEFR level is a shared scale, and stating B2 honestly is stronger than an unverifiable superlative." },

    { q: "Where should a number appear in a CV bullet?",
      a: ["Wherever one honestly exists, and nowhere it does not",
          "In every bullet, estimating where necessary",
          "Only in bullets about performance work",
          "Only where the figure is publicly verifiable"],
      c: 0,
      why: "Numbers make a claim concrete and inviteable. Inventing them is worse than having none, because the follow-up question in the interview exposes it immediately, and the whole document's credibility goes with it." },

    { q: "How should a personal project such as this academy be presented?",
      a: ["As engineering with decisions and trade-offs, not as a tutorial you followed",
          "As professional experience, to fill a gap",
          "Only if no commercial experience exists",
          "As a link, without description, to save space"],
      c: 0,
      why: "What makes a project interesting to an interviewer is the reasoning: why an integrity gate, why append-only ids, what you chose not to build. That is indistinguishable from real engineering, whereas 'built a site with Laravel' is indistinguishable from a course." },

    { q: "What is the strongest use of a LinkedIn headline?",
      a: ["The role and the specific stack, using the words adverts in your market use",
          "An aspirational statement about your goals",
          "Your current job title exactly as your employer writes it",
          "A list of every technology you have touched"],
      c: 0,
      why: "Recruiters search on terms, and the headline is weighted in that search. 'Full-stack developer, PHP and Laravel, Bologna' matches how they actually look for people, and it is the line that appears beside your name everywhere." },

    { q: "Why should a CV mirror the advert's own vocabulary?",
      a: ["Both a keyword filter and a human screener match against the words they wrote",
          "It shows you read the advert carefully",
          "It is required for the application to be logged",
          "It prevents the recruiter from misclassifying your experience"],
      c: 0,
      why: "This is only worth doing where it is true. Writing Laravel because you have used Laravel is matching their language; writing it because they asked for it is a claim that collapses in the first technical question." },
  ],

  /* ------------------------------------------------------- chapter 37 --- */
  "37-screening-test": [
    { q: "What is the single most important property of a take-home submission?",
      a: ["It runs from a clean clone, verified in a fresh directory",
          "It uses the most modern language features available",
          "It includes complete test coverage",
          "It is submitted well before the deadline"],
      c: 0,
      why: "A reviewer who cannot start it forms an opinion before reading a line. Local state, an untracked .env or a missing migration are all invisible from your machine, which is why cloning into a new directory and following your own README is the check that matters." },

    { q: "The exercise suggests four hours and you are not finished. What should you do?",
      a: ["Stop, submit what works, and write down what you would do next and why",
          "Continue until it is complete, however long it takes",
          "Submit an unfinished version with no explanation",
          "Ask for an extension"],
      c: 0,
      why: "Overrunning tells them nothing about how you work under a real constraint, and it is unfair to candidates who respected it. A clear note about what you deliberately left and what you would add reads as judgement rather than as an excuse." },

    { q: "What belongs in the README of a take-home exercise?",
      a: ["How to run it, the decisions you made and why, and what you deliberately left out",
          "A description of every class and method",
          "The full requirements as restated from the brief",
          "A changelog of your commits"],
      c: 0,
      why: "It is the highest-value twenty minutes of the whole exercise, because it is where reasoning becomes visible. Code shows what you did; the README is the only place you can explain why, which is the thing being assessed." },

    { q: "Why does saying where you stopped improve a submission?",
      a: ["Restraint under a timebox reads as judgement, and it shows you saw the missing parts",
          "It lowers the reviewer's expectations",
          "It shortens the review",
          "It transfers responsibility for the gaps"],
      c: 0,
      why: "Reviewers notice the missing rate limiting or the absent pagination anyway. Naming it turns 'did not think of it' into 'considered it and prioritised', which is exactly the distinction between a junior and a mid-level engineer." },

    { q: "How much testing does a take-home exercise warrant?",
      a: ["A few real tests on the parts with actual rules, rather than coverage of everything",
          "Full coverage, including getters and setters",
          "None, since the exercise is about the feature",
          "Only end-to-end tests, since they prove it works"],
      c: 0,
      why: "Testing trivial code demonstrates you can write test syntax. Testing the discount calculation, the state transition, the boundary condition, demonstrates that you know what is worth protecting, which is the more interesting signal." },

    { q: "What most often separates a good submission from an average one?",
      a: ["Handling the edge cases, because most submissions handle only the happy path",
          "Using a more advanced framework",
          "Including a Docker setup",
          "Writing more code overall"],
      c: 0,
      why: "Empty input, a duplicate submission, a missing external service, a zero quantity: these are where real software lives and where most submissions simply stop. Handling a few deliberately, and saying so, is cheap differentiation." },

    { q: "You are stuck during a live coding session. What should you do?",
      a: ["Narrate your thinking and say what you would look up, since silence is the real failure",
          "Work quietly until you have a working answer",
          "Ask to switch to a different problem",
          "Explain that you would normally use an AI assistant"],
      c: 0,
      why: "They are watching how you think, and a silent screen shows them nothing. Saying what you suspect, what you would test next and what you would check in the docs is a working answer to the question actually being asked." },

    { q: "In a pair-programming interview, what is being assessed beyond correctness?",
      a: ["Whether working with you is productive, including how you take a suggestion",
          "How quickly you type",
          "Whether you know the standard library by heart",
          "Whether you use the same editor as the team"],
      c: 0,
      why: "It is a simulation of a Tuesday. Do you hear an idea, engage with it, disagree constructively? Candidates who treat it as a solo exam under observation miss that the interviewer is deliberately participating." },

    { q: "When is it reasonable to decline a take-home exercise?",
      a: ["When it is disproportionate, such as days of work or building something they will use",
          "Whenever a technical interview is offered as an alternative",
          "When it uses a framework you do not know",
          "When it is timed rather than open-ended"],
      c: 0,
      why: "There is a real line between assessment and unpaid work, and a three-day project with production-shaped requirements is over it. Declining politely while offering a live session or existing code is a reasonable professional position." },

    { q: "What should you prioritise when the timebox is tight?",
      a: ["Working code with clear structure, over clever abstractions",
          "A complete architecture, even if some parts do not run",
          "Maximum use of the framework's advanced features",
          "The largest number of requirements attempted"],
      c: 0,
      why: "Working first, clever never. A submission that runs and is easy to read beats a partially finished demonstration of patterns, and half-built abstractions are read as complexity you could not finish rather than as ambition." },
  ],

  /* ------------------------------------------------------- chapter 38 --- */
  "38-the-interview": [
    { q: "How long should the answer to 'mi parli di lei' be, and what should it contain?",
      a: ["About ninety seconds, with deliberate hooks you want to be asked about",
          "Five minutes, covering your career chronologically",
          "Thirty seconds, since the CV already has the detail",
          "As long as needed to cover every role"],
      c: 0,
      why: "It sets the frame for everything after it, and it is the one answer you can fully prepare. Planting two or three specifics you want to discuss steers the conversation onto ground you have thought about." },

    { q: "What is the strongest structure for answering a technical question?",
      a: ["The answer, then the mechanism, then a concrete example from your own work",
          "A definition, then a textbook example",
          "A question back to clarify what they mean",
          "The answer, then the alternatives you rejected"],
      c: 0,
      why: "Most candidates produce the definition, so the example is the differentiator. 'We hit exactly that with an N+1 on the order list, and here is how we found it' proves the knowledge is yours rather than recently memorised." },

    { q: "You are asked something you genuinely do not know. What is the best response?",
      a: ["Say so, then describe how you would find out and reason from what you do know",
          "Give your best guess confidently",
          "Redirect to a related topic you know well",
          "Say you would look it up and stop there"],
      c: 0,
      why: "Interviewers routinely ask something out of range on purpose, and bluffing is transparent to anyone who knows the answer. Not knowing is expected; being unable to reason about it, or pretending, is what actually costs the offer." },

    { q: "You think their technology choices are poor. How should you handle it?",
      a: ["Ask about them as curiosity, since the same knowledge lands entirely differently",
          "Say diplomatically that you would have chosen otherwise",
          "Avoid the topic completely",
          "Offer a short critique to show technical depth"],
      c: 0,
      why: "The person opposite may have made that decision under constraints you cannot see. 'What made you go with X?' shows the same understanding as a critique and opens a conversation instead of a defence." },

    { q: "What does RAL mean in an Italian offer?",
      a: ["Gross annual salary, spread over thirteen or fourteen monthly instalments",
          "Net annual salary after tax and contributions",
          "The monthly gross salary",
          "The total cost to the employer including contributions"],
      c: 0,
      why: "Whether it is thirteen or fourteen months changes the monthly figure materially, so it must be asked. Comparing an offer's RAL with a colleague's net is a common error that makes an offer look better or worse than it is." },

    { q: "Beyond the RAL, which contract details should you ask about?",
      a: ["The CCNL and the livello, plus the contract type such as indeterminato or apprendistato",
          "Only the probation period length",
          "The company's revenue and headcount",
          "Which pension fund is used"],
      c: 0,
      why: "The CCNL and livello determine holidays, notice, minimums and progression, and apprendistato has its own pay and duration rules. These are ordinary questions here, and asking them signals that you understand what you are being offered." },

    { q: "How should you answer a question about salary expectations?",
      a: ["With a researched range and a brief reason for it",
          "With a single low figure to stay competitive",
          "By asking them to state their budget first, always",
          "By deferring the discussion until an offer is made"],
      c: 0,
      why: "A single low number becomes the ceiling immediately. A range with a reason, based on the market and the role's scope, is a negotiating position rather than a request, and it invites a conversation instead of a decision." },

    { q: "Which closing question most often rescues an application?",
      a: ["Asking whether they have any reservations about your profile",
          "Asking when they will decide",
          "Asking what the team's biggest challenge is",
          "Asking about opportunities for growth"],
      c: 0,
      why: "It surfaces the objection while you are still in the room and can answer it. Unaddressed doubts otherwise become a quiet rejection, and you never learn that a two-sentence clarification would have settled it." },

    { q: "What should you do immediately after an interview?",
      a: ["Write down every question you could not answer well, and treat it as your study plan",
          "Send a thank-you email restating your interest",
          "Ask for feedback on your performance",
          "Update your CV with anything you forgot to mention"],
      c: 0,
      why: "That list is the most accurate assessment of your gaps you will ever get, produced by someone whose job is finding them. Candidates who keep it improve measurably across a few interviews; candidates who do not repeat the same interview." },

    { q: "Which question about the role tells you most about daily life there?",
      a: ["What a typical week looks like, and how work reaches a developer",
          "How many employees the company has",
          "Which frameworks the team prefers",
          "Whether remote work is permitted"],
      c: 0,
      why: "It surfaces the things nobody advertises: whether requirements arrive written or shouted, whether there is review, whether you will be on support. Those determine whether you will enjoy the job far more than the stack does." },
  ],
});
