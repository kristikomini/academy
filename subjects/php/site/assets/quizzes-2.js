/* ==========================================================================
   quizzes-2.js — the question bank, part 2.

   Same rules as part 1, and they are worth repeating because breaking them is
   silent:

   APPEND ONLY. A question's id is "<chapter-id>#<index>" and the index is its
   position in this array. That id keys a learner's spaced-repetition schedule.
   Reordering or deleting reassigns somebody's review history to the wrong
   questions and nothing tells you it happened.

   `why` is shown on correct answers too.
   ========================================================================== */

window.QUIZZES = Object.assign(window.QUIZZES || {}, {

  /* ------------------------------------------------------- chapter 01 --- */
  "01-php-platform": [
    { q: "What does PHP-FPM's process model mean for application state?",
      a: ["Nothing survives between requests — each is handled by a worker with a clean memory space",
          "State is shared across all workers automatically",
          "Each worker keeps its own persistent application state",
          "State survives only within the same worker"],
      c: 0,
      why: "This is 'shared-nothing', PHP's biggest structural difference from Java or .NET. It explains why there is no application-level connection pool, why 'just make it static' is almost always wrong, and why deployment is as simple as replacing files." },

    { q: "What does OPcache actually cache?",
      a: ["The compiled bytecode of your PHP files",
          "The HTML output of each page",
          "Database query results",
          "Composer's autoload map"],
      c: 0,
      why: "Without it PHP re-parses and re-compiles every file on every request. OPcache keeps the compiled opcodes in shared memory, which is typically the single largest performance setting on a production PHP server. It caches code, not data — that is what Redis is for." },

    { q: "Why does composer.lock belong in version control?",
      a: ["It pins the exact dependency versions, so production installs what you tested",
          "It speeds up autoloading",
          "Composer refuses to run without it",
          "It contains the PSR-4 mappings"],
      c: 0,
      why: "composer.json states acceptable ranges; composer.lock records the exact resolved versions. Commit the lock and run `composer install` (not `update`) in CI and production, otherwise a patch release you never tested can ship on a Friday." },

    { q: "What is the difference between the CLI and FPM SAPIs regarding php.ini?",
      a: ["They commonly load different php.ini files, so settings can differ between them",
          "They always share one configuration",
          "CLI ignores php.ini entirely",
          "FPM has no memory limit"],
      c: 0,
      why: "A classic lost afternoon: a script works via `php script.php` and fails through the web server, or vice versa. Run `php --ini` and check `phpinfo()` from the web to see which files each SAPI actually loaded. CLI also defaults to no max_execution_time." },

    { q: "What does the PHP-FPM setting pm.max_children actually bound?",
      a: ["The number of requests that can be served concurrently by that pool",
          "The number of CPU cores the pool may use",
          "The maximum memory a single request may allocate",
          "The number of requests each worker handles before recycling"],
      c: 0,
      why: "It is a concurrency ceiling, and it must be sized by available RAM divided by the average process size rather than set optimistically. Too high and the server swaps under load; too low and requests queue while memory sits unused." },

    { q: "Why must every outbound HTTP call from a PHP-FPM application have a timeout?",
      a: ["A blocked worker serves nobody, so a slow dependency consumes the whole pool",
          "PHP raises a fatal error if a socket stays open too long",
          "Without one, the response is never flushed to the client",
          "Timeouts are required for keep-alive connections to be reused"],
      c: 0,
      why: "With max_children workers and a partner API hanging for sixty seconds, the pool fills and the site stops responding to everyone, including the pages that do not touch that API. Someone else's outage becomes yours, and a timeout is the whole defence." },

    { q: "With opcache.validate_timestamps set to 0, what must a deployment do?",
      a: ["Reload PHP-FPM, or the old bytecode keeps being served",
          "Clear the browser cache for all users",
          "Run composer dump-autoload before switching the symlink",
          "Nothing, since OPcache detects the new file inode"],
      c: 0,
      why: "Disabling timestamp validation removes a stat call per file per request, which is a real gain, and it means PHP never notices your new code. The symptom is a deploy that appears to have done nothing at all, which sends people hunting in entirely the wrong place." },

    { q: "What should display_errors be set to in production, and why?",
      a: ["Off, with errors written to a log instead, because error output can reveal paths and credentials",
          "On, so that failures are visible to users who report them",
          "On, but only for requests from internal IP addresses",
          "Off in the CLI and On for FPM"],
      c: 0,
      why: "An uncaught exception rendered to the page can print file paths, SQL fragments and connection details. Log it, monitor the log, and show the user a neutral error page. This is one of the settings most commonly left wrong on Italian shared hosting." },

    { q: "What effect does enabling the JIT have on a typical PHP web application?",
      a: ["Very little, because such applications are bound by input and output rather than by CPU",
          "It roughly doubles request throughput",
          "It removes the need for OPcache",
          "It reduces memory usage per request"],
      c: 0,
      why: "The JIT helps CPU-bound numeric work, which is not what a request waiting on MySQL and an API is doing. Knowing where a feature does and does not apply is a better answer than reciting that PHP 8 has a JIT." },

    { q: "Why does max_execution_time behave differently under the CLI SAPI?",
      a: ["It defaults to unlimited there, because a script is not a web request that must not hang",
          "The CLI ignores php.ini entirely",
          "The CLI applies it per function call rather than per script",
          "It is enforced by the web server rather than by PHP"],
      c: 0,
      why: "CLI and FPM are genuinely different worlds with their own ini files and their own defaults. It is why a migration or an import runs happily from the command line and times out when somebody triggers it through a controller." },
  ],

  /* ------------------------------------------------------- chapter 10 --- */
  "10-sql-querying": [
    { q: "What happens to a LEFT JOIN when you filter the right-hand table in WHERE?",
      code: "SELECT c.*, o.total\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nWHERE o.total > 100;",
      a: ["It behaves as an INNER JOIN — customers with no orders are removed",
          "It still returns all customers, with NULL totals",
          "It returns a syntax error",
          "It returns only customers with no orders"],
      c: 0,
      why: "The unmatched rows have o.total = NULL, and NULL > 100 is not true, so WHERE discards them — silently undoing the LEFT JOIN. To keep them, move the condition into the ON clause, or add OR o.total IS NULL. This is the most common SQL bug in interviews and in production." },

    { q: "What is the difference between WHERE and HAVING?",
      a: ["WHERE filters rows before grouping; HAVING filters groups after aggregation",
          "HAVING is only valid on indexed columns",
          "They are interchangeable",
          "WHERE works on joins, HAVING on subqueries"],
      c: 0,
      why: "WHERE cannot see aggregates because they do not exist yet; HAVING can. Filter as much as possible in WHERE — it reduces the rows that ever reach the grouping step, so it is both correct and faster." },

    { q: "Which construct returns the top 3 rows per category in one query?",
      a: ["A window function: ROW_NUMBER() OVER (PARTITION BY category ORDER BY total DESC)",
          "GROUP BY category LIMIT 3",
          "DISTINCT ON category with LIMIT",
          "Three separate UNION ALL queries"],
      c: 0,
      why: "GROUP BY collapses each category to one row, so LIMIT 3 gives three categories, not three rows each. ROW_NUMBER() numbers rows within each partition, then you filter that number in an outer query or CTE. This is a very common live-coding exercise." },

    { q: "What is the practical difference between EXISTS and IN with a subquery?",
      a: ["EXISTS stops at the first match and handles NULLs safely; NOT IN returns nothing if the subquery yields any NULL",
          "IN is always faster",
          "EXISTS cannot use an index",
          "They are identical in every engine"],
      c: 0,
      why: "The NULL behaviour is the trap: NOT IN (1, 2, NULL) is never true for anything, because comparing to NULL is unknown. So a NOT IN over a nullable column silently returns an empty set. NOT EXISTS does not have this problem." },

    { q: "What does a CTE (WITH clause) give you?",
      a: ["A named, readable subquery you can reference — and, with RECURSIVE, hierarchy traversal",
          "A guaranteed performance improvement",
          "A temporary table persisted between sessions",
          "Automatic indexing of the intermediate result"],
      c: 0,
      why: "The main win is readability: a three-level nested subquery becomes three named steps. Recursive CTEs walk trees, which is how you query a category hierarchy or a bill of materials. Do not assume a performance gain — MySQL may materialise it." },

    { q: "After a LEFT JOIN, why does COUNT(orders.id) usually give the answer you wanted rather than COUNT(*)?",
      a: ["COUNT(*) counts rows including unmatched ones, while COUNT(col) skips NULLs",
          "COUNT(*) is not permitted with GROUP BY",
          "COUNT(col) is faster because it can use the index",
          "COUNT(*) counts only distinct rows"],
      c: 0,
      why: "A customer with no orders still produces one row from the LEFT JOIN, with NULLs on the right. COUNT(*) reports 1 for them; COUNT(orders.id) reports 0. Reporting every customer as having at least one order is a bug nobody notices until somebody checks." },

    { q: "A NOT IN subquery returns no rows at all, although you expected many. What is the likely cause?",
      a: ["The subquery returned a NULL, which makes every NOT IN comparison unknown",
          "The subquery returned too many rows for the optimiser",
          "NOT IN requires an index on the compared column",
          "The subquery was correlated and evaluated once"],
      c: 0,
      why: "Comparing anything with NULL yields unknown, not false, so NOT IN can never be satisfied once a NULL is present in the list. NOT EXISTS expresses the same intent without the trap, which is why it is the recommended form." },

    { q: "You want rows where a ROW_NUMBER() is 1. Why can you not put that in the WHERE clause?",
      a: ["Window functions are evaluated after WHERE, so the value does not exist yet",
          "Window functions may only appear in ORDER BY",
          "WHERE cannot reference any computed column",
          "The window frame would be recalculated per row"],
      c: 0,
      why: "The order of evaluation is FROM, WHERE, GROUP BY, then window functions, then SELECT. Wrap the query in a CTE and filter on the outside, which is also the standard shape for the top-N-per-group problem." },

    { q: "Under ONLY_FULL_GROUP_BY, why is selecting a non-aggregated column that is not in the GROUP BY rejected?",
      a: ["The database cannot know which row's value to return for the group",
          "It would prevent the use of an index",
          "The SQL standard forbids more than one column in GROUP BY",
          "It causes ambiguity only when a JOIN is present"],
      c: 0,
      why: "Older MySQL picked an arbitrary row and returned its value, which produced quietly wrong reports. The strict mode makes you say what you meant, with MIN, MAX, ANY_VALUE or a wider GROUP BY, and it is on by default in MySQL 5.7 and later." },

    { q: "What does an INNER JOIN produce for a row on the left with no match on the right?",
      a: ["Nothing, because the row is excluded from the result entirely",
          "One row with NULLs in the right-hand columns",
          "One row with default values in the right-hand columns",
          "An error, unless the join is declared as OUTER"],
      c: 0,
      why: "This is why a report that should list every customer loses the ones with no orders the moment somebody changes LEFT to INNER, and why filtering a LEFT JOIN's right-hand table in WHERE has exactly the same effect without saying so." },
  ],

  /* ------------------------------------------------------- chapter 16 --- */
  "16-the-framework": [
    { q: "In a Laravel middleware, when does code AFTER $next($request) run?",
      a: ["On the way out, once the response has been produced by everything deeper in the stack",
          "Before the controller, immediately after the code above it",
          "Only if the request failed",
          "It never runs — code after $next is unreachable"],
      c: 0,
      why: "Middleware is an onion, not a queue. Code before $next() runs inbound; $next() dives through the remaining middleware and the controller; code after it runs outbound with the response in hand. That is how you add a timing header or log a status code." },

    { q: "How does Laravel's service container decide what to inject into a constructor?",
      a: ["It reads the parameter type hints and resolves each one recursively",
          "It matches parameter names against config keys",
          "It injects everything registered, in order",
          "It requires an explicit annotation on each parameter"],
      c: 0,
      why: "Type hints drive autowiring. A concrete class it can just build; an interface needs a binding to say which implementation. That binding is the seam that makes the code swappable and testable — the practical payoff of dependency inversion." },

    { q: "What is the lifetime of a Laravel singleton?",
      a: ["One HTTP request — PHP is shared-nothing, so nothing persists between requests",
          "The lifetime of the server process, shared by all users",
          "Until the cache is cleared",
          "One session"],
      c: 0,
      why: "singleton() means 'resolve once per request', not 'once per application' as it would in Java or .NET. Candidates arriving from those languages get this wrong, and it matters: you cannot use a Laravel singleton as a cross-request cache." },

    { q: "What is a Laravel facade, technically?",
      a: ["A static proxy that resolves the real object from the container and forwards the call",
          "A class of static methods",
          "A singleton with a shorter name",
          "A trait providing static access"],
      c: 0,
      why: "Cache::get() is not a static call on a Cache class — it resolves the bound instance and forwards. That is why facades are mockable in tests. The cost is hidden dependencies: a class using facades declares nothing in its constructor, so its real dependencies are invisible." },

    { q: "What does a Form Request's authorize() method control?",
      a: ["Whether the user is permitted to make this request at all — returning false produces a 403",
          "Whether the validation rules run",
          "Whether the route is registered",
          "Which middleware group applies"],
      c: 0,
      why: "authorize() answers 'may you?' and rules() answers 'is the input valid?'. They are separate questions with separate failure codes — 403 versus 422 — and both are resolved before your controller method is entered." },

    { q: "Where should validation live in a Laravel application?",
      a: ["In a Form Request, so the controller only ever receives valid input",
          "In the model's boot method",
          "In the controller, at the top of each method",
          "In middleware, manually"],
      c: 0,
      why: "A form request runs before the controller and returns 422 with field errors automatically. It keeps controllers thin, makes the rules reusable and testable in isolation, and means the controller body never begins with fifteen lines of if-statements." },

    { q: "Why is the order of middleware in the stack a behavioural decision rather than a stylistic one?",
      a: ["Each wraps the next, so authentication placed after a handler runs too late to protect it",
          "The framework executes them alphabetically regardless of registration",
          "Order only affects performance, not correctness",
          "Middleware order determines which routes they apply to"],
      c: 0,
      why: "It is an onion: everything before the call to the next handler runs on the way in, everything after runs on the way out. Put the rate limiter inside the expensive work and it limits nothing; put a session writer before the session starts and it writes nothing." },

    { q: "What do you gain by binding an interface in the container rather than type-hinting a concrete class?",
      a: ["A seam, so the implementation can be swapped in tests or replaced without touching consumers",
          "Faster resolution, because interfaces are cached",
          "Automatic validation of the implementation's methods",
          "The ability to bind more than one implementation at once"],
      c: 0,
      why: "This is dependency inversion made practical. The consumer states what it needs, the container decides what satisfies it, and a fake in a test is one binding away. Type-hinting the concrete vendor class works until the day you need it not to." },

    { q: "What is the relationship between Laravel and Symfony?",
      a: ["Laravel is built on Symfony components, including the HTTP foundation and console",
          "They are entirely separate implementations with no shared code",
          "Symfony is built on Laravel's components",
          "They share only the Composer packaging convention"],
      c: 0,
      why: "Routing, the request and response objects, the console and the event dispatcher have Symfony underneath. Treating the two as opposing camps misses that experience in one transfers substantially to the other, which is worth saying in an interview." },

    { q: "Where does Symfony most visibly differ from Laravel in daily use?",
      a: ["It favours explicit configuration and service wiring over convention and facades",
          "It has no dependency injection container",
          "It cannot use Doctrine or an ORM",
          "It does not support middleware"],
      c: 0,
      why: "Symfony asks you to say what you mean in configuration and attributes, which is more setup and fewer surprises. Laravel optimises for speed of writing with conventions and facades. Neither is correct in general, and both appear in this market's adverts." },
  ],


  /* ------------------------------------------------------- chapter 00 --- */
  "00-the-job-posting": [
    { q: "In an Italian job advert, what is the practical difference between a skill listed as 'gradita' and one listed as 'consolidata'?",
      a: ["Gradita is a nice-to-have; consolidata means proven, years-deep experience they will test",
          "They are synonyms, both meaning required",
          "Gradita means required by law; consolidata means optional",
          "Gradita applies to soft skills, consolidata to technical ones"],
      c: 0,
      why: "The qualifier carries more information than the noun. 'Laravel gradito' and 'consolidata esperienza Laravel' describe two different jobs and two different interviews. Read the qualifier first: it tells you where to spend your preparation." },

    { q: "An advert says 'sviluppo e manutenzione di applicazioni web'. Which half of that line do candidates routinely fail to prepare for?",
      a: ["Manutenzione, which means reading and changing code somebody else wrote",
          "Sviluppo, which means writing new features from scratch",
          "Applicazioni, which means knowing the framework's API surface",
          "Web, which means HTTP status codes"],
      c: 0,
      why: "Most of the work in a software house is maintenance, and most candidates prepare only for greenfield questions. 'How do you approach a codebase you have never seen?' is a real interview question in this market, and an honest structured answer earns more than a memorised design pattern." },

    { q: "Roughly what proportion of an advert's requirement list should you match before applying is a reasonable use of your time?",
      a: ["About two thirds, because the list is a wish list rather than a gate",
          "All of it, or an applicant tracking system rejects you automatically",
          "Half, but only with a referral",
          "It does not matter, because adverts are written after the candidate is chosen"],
      c: 0,
      why: "Requirement lists are assembled by combining every wish from the team, and are rarely met in full even by the person eventually hired. Two thirds, with real depth on the lines carrying qualifiers like 'consolidata', is a candidate." },

    { q: "Which four abilities do PHP adverts almost never list, yet nearly every interview probes?",
      a: ["Debugging, working with legacy code, explaining your reasoning, and knowing where it deploys",
          "Design patterns, UML, algorithms, and Big-O analysis",
          "English, German, French, and Spanish",
          "Agile certification, Scrum Master, ITIL, and PRINCE2"],
      c: 0,
      why: "The advert lists technologies because technologies are easy to write down. The interview tests the four things that make somebody useful on a Tuesday: finding a bug, surviving somebody else's code, saying why you chose something, and knowing what happens after git push." },

    { q: "An advert requires 'autonomia nella gestione delle attivita assegnate'. What is it actually asking you to demonstrate?",
      a: ["That you can turn a vague request into a task and ask a good question when genuinely stuck",
          "That you prefer to work alone with no communication",
          "That you have managed a team before",
          "That you will need no onboarding at all"],
      c: 0,
      why: "Autonomia is not silence. It means somebody can hand you a half-specified ticket and get a working result without supervising each step, which requires asking the right question early rather than never asking." },

    { q: "What is the most useful way to unpack a single requirement line from an advert?",
      a: ["Into the concrete Tuesday it describes, meaning the tasks that line implies you will do",
          "Into a list of certifications that prove it",
          "Into the salary band it justifies",
          "Into a keyword to paste into your CV"],
      c: 0,
      why: "A requirement line is a task, not a skill. 'Conoscenza di MySQL' means writing queries that survive real data, reading a slow query, and choosing a column type. Prepare the tasks and the skill answers itself." },

    { q: "An advert from a Modena software house says 'sviluppo gestionali'. What does that one word tell you about the domain?",
      a: ["Business software with invoicing, VAT, sequential numbering and fiscal-system integration",
          "Management consulting rather than programming",
          "Game development",
          "A management position rather than a developer one"],
      c: 0,
      why: "A gestionale is the ERP-adjacent business software that a large slice of the Emilia-Romagna market builds and maintains. It means money arithmetic, state machines, legal invariants and fatturazione elettronica, which is why this course has a chapter on the SDI." },

    { q: "Two adverts describe the same thing as 'framework MVC' and as 'Laravel'. Which wording should your CV mirror?",
      a: ["The advert's own specific word, Laravel, because both a human screener and an ATS match on it",
          "The generic phrase, because it covers more adverts at once",
          "Neither, because a third term shows more breadth",
          "Whichever is shorter, to save space"],
      c: 0,
      why: "Applicant tracking systems match strings, and a screening reader scans for the exact word from their own advert. 'Framework PHP' matches nothing; 'Laravel' matches the line they wrote. Mirror their true words wherever they are true of you." },

    { q: "An advert lists twelve technologies you have never used and three you know well. What is the strongest interview preparation?",
      a: ["Depth on the emphasised lines, plus an honest account of how you would approach the rest",
          "A skim of all twelve so you can say something about each",
          "Declining the interview until you have learned all twelve",
          "Claiming familiarity with all fifteen and revising the night before"],
      c: 0,
      why: "Breadth on everything collapses under one follow-up question. Depth on the quoted lines plus a candid, structured answer for the gaps is exactly what a technical interviewer is trying to establish, and it survives probing." },

    { q: "Why does an advert's requirement list often include technologies the team barely uses?",
      a: ["It accumulates every team member's wishes and nobody edits it back down",
          "Legal requirements oblige a minimum list length",
          "Recruiters are paid per keyword",
          "It is a deliberate test of honesty"],
      c: 0,
      why: "Adverts accumulate. Someone adds Docker because they would like it one day, someone adds Vue because one project uses it. Treating the list as a specification to satisfy in full misreads how it was written." },
  ],

  /* ------------------------------------------------------- chapter 04 --- */
  "04-closures-and-generators": [
    { q: "In PHP, at what moment does a closure's use ($x) capture the value of $x?",
      a: ["When the closure is defined, by value",
          "When the closure is called, by value",
          "When the closure is called, by reference",
          "Only once the closure is bound to an object"],
      c: 0,
      why: "use ($x) copies the variable at definition time. Changing $x afterwards does not change what the closure sees. use (&$x) captures by reference instead and shares the variable, which is occasionally what you want and usually a bug." },

    { q: "What does a PHP arrow function, written with fn, capture from the enclosing scope?",
      a: ["Everything it uses, automatically and by value",
          "Nothing, so variables must be listed with use()",
          "Everything, by reference",
          "Only $this"],
      c: 0,
      why: "That is the whole contract of fn: automatic capture by value, and a single expression as the body. It is not a shorter function() with extra powers, so there are no statements, no explicit use list, and no capture by reference." },

    { q: "What is the practical difference between a generator and a function returning an array of the same values?",
      a: ["The generator produces values lazily, one at a time, in constant memory",
          "The generator is faster but uses the same memory",
          "The generator can be counted with count(), while the array cannot",
          "There is none, because yield is sugar for building an array"],
      c: 0,
      why: "A function returning an array must build the whole array in memory first. A generator suspends at each yield and resumes on the next iteration, so streaming a million-row export goes from O(n) memory to O(1). That is the entire argument for it." },

    { q: "Why can count() not be used on a PHP generator?",
      a: ["A generator does not know how many values it will produce until it has produced them",
          "count() only works on objects implementing ArrayAccess",
          "Generators are internally strings",
          "It can, because count() runs the generator to completion first"],
      c: 0,
      why: "A generator is a lazy, single-use sequence, so knowing its length would mean running it and destroying the reason for using one. Wanting count() usually means you wanted an array, or that the count belongs in a separate query." },

    { q: "A generator opens a file handle and yields lines, but the consumer breaks out of the foreach after ten lines. Where does the fclose() belong?",
      a: ["In a finally block wrapped around the yield loop",
          "After the loop, at the end of the generator body",
          "In the consumer, immediately after the break",
          "Nowhere, because PHP closes handles at request end"],
      c: 0,
      why: "Code after the loop never runs if the consumer abandons the generator early. finally does run when the generator is destroyed, which makes it the only reliable place for cleanup in a generator that may not be drained." },

    { q: "What happens when you iterate the same PHP generator instance a second time?",
      a: ["An exception, because a generator cannot be rewound once it has started",
          "It silently produces no values",
          "It restarts from the beginning",
          "It replays the values cached during the first pass"],
      c: 0,
      why: "Generators are single-use, and calling rewind() after the first yield throws. To iterate twice, call the generator function again for a fresh instance or collect it into an array, and if you must collect it, ask whether a generator was the right choice." },

    { q: "Which PHP callable syntax fails where it is written rather than where it is invoked?",
      a: ["First-class callable syntax, written as $obj->method(...)",
          "The string form, 'Class::method'",
          "The array form, [$obj, 'method']",
          "A variable variable holding the method name"],
      c: 0,
      why: "String and array callables are resolved at call time, so a typo survives until that line runs, often in production. $obj->method(...) is checked where it is written, static analysis understands it, and refactoring tools can rename it." },

    { q: "Why can a closure defined in a Laravel route file often use $this or reach container bindings?",
      a: ["The framework rebinds the closure's scope with bindTo before invoking it",
          "Closures always inherit the global object",
          "$this inside a closure defaults to the service container",
          "Route closures are compiled into methods at build time"],
      c: 0,
      why: "A closure carries a bound $this and a scope, and both can be replaced. Frameworks use bindTo to run your closure as if it were a method of their own object. It is not magic, it is a documented API, and knowing that is the difference between using a framework and understanding one." },

    { q: "What does yield from do inside a PHP generator?",
      a: ["Delegates to another traversable, yielding all of its values in sequence",
          "Returns the final value of another generator and stops",
          "Converts a generator into an array",
          "Yields one value and then terminates the generator"],
      c: 0,
      why: "yield from composes generators, letting you split a long pipeline into readable pieces without materialising the intermediate results. It preserves the inner generator's keys, which surprises anyone calling iterator_to_array without passing false." },

    { q: "Where in a typical PHP application do closures and generators most often earn their place?",
      a: ["Callbacks in collection pipelines, and streaming large exports or file reads",
          "Replacing classes entirely in domain code",
          "Making synchronous code concurrent",
          "Improving OPcache hit rates"],
      c: 0,
      why: "Those are the two everyday cases: a short closure passed to a higher-order function, and a generator that produces a 500 MB CSV without a 500 MB array. Neither makes code concurrent, which remains a queue's job." },
  ],

  /* ------------------------------------------------------- chapter 05 --- */
  "05-solid-and-patterns": [
    { q: "The Single Responsibility Principle says a class should have one reason to change. What is the most useful way to count those reasons?",
      a: ["Count the people or departments who can ask for a change to it",
          "Count its public methods and keep the number below seven",
          "Count its dependencies in the constructor",
          "Count the lines of code and split above three hundred"],
      c: 0,
      why: "A reason to change is a stakeholder, not a method. If accounting can ask for a change to the VAT rule and the warehouse can ask for a change to the packing rule, that class has two reasons to change no matter how few methods it has." },

    { q: "When is the right moment to introduce an abstraction over two similar pieces of code?",
      a: ["At the second case, because the second case shows you where the real seam is",
          "At the first case, before duplication can appear",
          "At the fourth case, following the rule of three",
          "Never, since abstraction always costs more than duplication"],
      c: 0,
      why: "Abstracting from one example encodes an accident as a rule. The second case is what reveals which parts genuinely vary. Abstracting too late is cheap to fix; abstracting too early produces an interface everybody must lie to." },

    { q: "A class implements an interface but throws BadMethodCallException from two of its five methods. Which principle has been violated?",
      a: ["Interface Segregation, and the exception is the violation reporting itself",
          "Single Responsibility, because the class does too much",
          "Open/Closed, because the class had to be modified",
          "Dependency Inversion, because it depends on a concrete type"],
      c: 0,
      why: "An implementer forced to reject part of a contract is being made to depend on methods it does not use, which is exactly what Interface Segregation forbids. The throw is diagnostic: split the interface along the line the exception drew." },

    { q: "In Dependency Inversion, who should own the interface a high-level module depends on?",
      a: ["The consumer, the high-level module, because that ownership is what makes it inversion",
          "The implementation package, so it can evolve freely",
          "A shared contracts package owned by nobody in particular",
          "The framework, which supplies the standard interfaces"],
      c: 0,
      why: "If the low-level module defines the interface, the dependency still points from high to low and nothing has been inverted. The consumer states what it needs, and the detail conforms to it. That ownership is the whole idea." },

    { q: "What is the correct relationship between DIP, dependency injection, and a service container?",
      a: ["DIP is the principle, dependency injection is the mechanism, the container is the tool",
          "They are three names for the same thing",
          "DIP is the tool, DI is the principle, the container is the mechanism",
          "DIP applies to classes, DI to functions, containers to frameworks"],
      c: 0,
      why: "Conflating them is a common interview stumble. You can practise dependency injection with no container at all by passing collaborators into a constructor, and you can use a container while thoroughly violating DIP by type-hinting concrete classes throughout." },

    { q: "When is an interface with exactly one implementation and no test double genuine ceremony?",
      a: ["When nothing else will ever implement it and no test needs to substitute it",
          "Always, since one implementation means the abstraction is wrong",
          "Never, because every class should sit behind an interface",
          "Only in code that does not use a dependency injection container"],
      c: 0,
      why: "An interface earns its place by giving you a seam: a second implementation, a fake in a test, or a boundary you own against a vendor you do not. With none of those present it adds a file and a jump for no benefit, and it can be added the day a seam is needed." },

    { q: "Asked 'quali design pattern conosce?' in an interview, what is the strongest answer?",
      a: ["Three you have genuinely used, where and why, plus one you deliberately did not use",
          "As long a list as you can recall, to show breadth",
          "Only the Gang of Four creational patterns, which are the classics",
          "That patterns are outdated and dependency injection replaced them"],
      c: 0,
      why: "A list of eleven invites 'implement Visitor on the whiteboard'. Three you have used with a concrete situation shows judgement, and naming one you rejected shows you evaluate rather than collect. Enthusiasm is what makes this question go wrong." },

    { q: "Which everyday Laravel feature is an application of the Strategy pattern?",
      a: ["Binding an interface to a chosen implementation and swapping it by configuration",
          "The Blade template compiler",
          "Eloquent's query builder chaining",
          "The routing table"],
      c: 0,
      why: "Swapping a payment gateway or a filesystem driver by changing the binding is Strategy: an interchangeable algorithm chosen at runtime behind a common contract. Recognising the patterns a framework already made you use is more convincing than reciting a catalogue." },

    { q: "What does the Open/Closed Principle ask of a class that must support a new payment provider?",
      a: ["That the new provider can be added as a new implementation, without surgery on existing code",
          "That the class be marked final so nothing can change it",
          "That every method be public so it can be extended",
          "That the class be rewritten to include the new provider's logic"],
      c: 0,
      why: "Open for extension, closed for modification: adding behaviour should mean adding a class and a binding, not editing a switch statement that already works and is already tested. A growing match on a provider name is the smell this principle names." },

    { q: "What does the Liskov Substitution Principle actually forbid in a subclass?",
      a: ["Strengthening what the caller must provide or weakening what the caller is promised",
          "Overriding any method inherited from the parent",
          "Adding new public methods not present on the parent",
          "Declaring the subclass final"],
      c: 0,
      why: "Substitutability is about the contract, not the signature. A subclass that throws where the parent returned, or that rejects inputs the parent accepted, breaks callers written against the parent even though the code compiles and the types line up." },
  ],

  /* ------------------------------------------------------- chapter 06 --- */
  "06-arrays-and-collections": [
    { q: "What single fact about the PHP array explains most of its surprising behaviour?",
      a: ["It is one ordered hash map doing the job of list, dictionary, set, stack and queue",
          "It is a linked list optimised for prepending",
          "It is a fixed-size C array that reallocates on growth",
          "It is internally an object implementing ArrayAccess"],
      c: 0,
      why: "Every surprise follows from that one structure: keys can be integers or strings, insertion order is preserved, a filtered array keeps its original keys, and json_encode turns a gapped list into an object. There is no separate list type to fall back on." },

    { q: "You call array_filter() on a list and pass the result to json_encode(). Why might the JSON come back as an object rather than an array?",
      a: ["array_filter preserves keys, so the gaps make it no longer a 0..n-1 list",
          "json_encode always produces objects unless given JSON_FORCE_ARRAY",
          "array_filter returns an ArrayObject rather than an array",
          "The callback returned strings rather than booleans"],
      c: 0,
      why: "json_encode emits a JSON array only when the keys are exactly 0..n-1 in order. Filtering leaves holes, so you get an object keyed by the surviving indexes, and a JavaScript client that expected to map over a list breaks. Call array_values() after any filter." },

    { q: "Why is in_array($needle, $haystack) an O(n) operation while isset($haystack[$key]) is O(1)?",
      a: ["The hash map indexes keys, not values, so searching by value must scan",
          "in_array is implemented in PHP rather than C",
          "isset skips the type check that in_array performs",
          "in_array sorts the array before searching it"],
      c: 0,
      why: "It is the same reason a book index finds a word instantly but finding every page mentioning a colour means reading the book. When you must repeatedly ask whether a value is present, flip the array with array_flip() and ask about a key." },

    { q: "Why should in_array() nearly always be called with its third argument set to true?",
      a: ["Without strict mode it compares loosely, so surprising values match",
          "Without it the function returns the index rather than a boolean",
          "Without it the search is case-insensitive",
          "Without it the function only searches the first hundred elements"],
      c: 0,
      why: "Loose comparison inside in_array is where 0 matches an unexpected string, or a numeric string matches an integer. Strict mode compares type and value, which is what you almost always meant, and it costs nothing." },

    { q: "After iterating an array with foreach ($items as &$item), what must you do next and why?",
      a: ["unset($item), because the reference survives the loop and the next write clobbers the last element",
          "Nothing, because references are scoped to the loop",
          "Call array_values(), because references renumber the keys",
          "Reassign the array to itself to break the reference"],
      c: 0,
      why: "After the loop, $item is still a reference to the final element. A second foreach reusing $item writes each value into that last slot, and the array quietly ends with its penultimate value duplicated. It is one of the oldest PHP bugs and it never throws." },

    { q: "PHP arrays are assigned by value. Why is passing a large array to a function still cheap?",
      a: ["Copy-on-write means the copy happens only if one side writes to it",
          "PHP passes arrays by reference internally regardless of syntax",
          "The engine caches array copies in OPcache",
          "Arrays above a size threshold are automatically passed by handle"],
      c: 0,
      why: "The array shares its internal buffer until somebody modifies it, at which point the real copy happens. This is why adding & for speed is a myth with a mutation bug attached: you gain nothing measurable and you gain the ability to corrupt the caller's data." },

    { q: "When should you reach for array_key_exists() rather than isset()?",
      a: ["When null is a meaningful stored value, because isset returns false for it",
          "When the array has string keys rather than integer keys",
          "When the array is nested more than two levels deep",
          "When performance matters, since array_key_exists is faster"],
      c: 0,
      why: "isset answers 'is there a non-null value here', which conflates a missing key with a stored null. If null means something in your data, such as a nullable column read from the database, only array_key_exists tells the truth." },

    { q: "Which array_* function turns a list of rows into a map keyed by one of their columns?",
      a: ["array_column, given the column to take and the column to key by",
          "array_combine, which requires equal-length key and value arrays",
          "array_chunk, which splits it into groups",
          "array_fill_keys, which assigns one value to many keys"],
      c: 0,
      why: "array_column($rows, null, 'id') keys the rows by id, and array_column($rows, 'name', 'id') builds an id to name map. It replaces the foreach everyone writes by hand, and the keyed result turns later O(n) lookups into O(1)." },

    { q: "When is an SplObjectStorage a better choice than a plain array?",
      a: ["When you need a set of objects, since objects cannot be array keys",
          "When you need faster integer-keyed iteration",
          "When the collection must be serialised to JSON",
          "When the array would exceed the memory limit"],
      c: 0,
      why: "PHP array keys are integers or strings only, so 'have I already seen this object' cannot be expressed with a plain array without inventing an id. SplObjectStorage attaches objects directly, and SplQueue or SplFixedArray earn their place for similar structural reasons." },

    { q: "What does usort() do to the array's keys?",
      a: ["It discards them and renumbers from zero",
          "It preserves them, matching asort()",
          "It preserves them only for string keys",
          "It throws if the array has string keys"],
      c: 0,
      why: "usort reindexes. If the keys carried meaning, such as an id, you needed uasort instead. Sorting a keyed collection with usort and then wondering where the ids went is a very ordinary afternoon's bug." },
  ],

  /* ------------------------------------------------------- chapter 07 --- */
  "07-errors-and-async": [
    { q: "In PHP 7 and later, what is the relationship between Error and Exception?",
      a: ["Both implement Throwable, but Error is not a subclass of Exception",
          "Error extends Exception, so catching Exception catches both",
          "Exception extends Error, so catching Error catches both",
          "Error is an interface implemented by Exception"],
      c: 0,
      why: "They are siblings under Throwable. This is why catch (Exception $e) silently misses a TypeError or a DivisionByZeroError. At an application boundary, catch Throwable; inside your domain, catch the narrowest type you can actually act on." },

    { q: "A PHP warning is raised, such as an undefined array key. What happens to it by default?",
      a: ["It is reported and execution continues, unless an error handler converts it to an exception",
          "It is thrown as an ErrorException automatically",
          "It aborts the request with a 500",
          "It is silently discarded in all versions"],
      c: 0,
      why: "Warnings are not exceptions until you make them so. set_error_handler that throws ErrorException is how a team stops discovering warnings months later in a log nobody reads. Frameworks such as Laravel install exactly that handler for you." },

    { q: "Which mechanism is the only way to observe a genuine fatal error, such as exhausting the memory limit?",
      a: ["A shutdown function registered with register_shutdown_function, inspecting error_get_last()",
          "A try/catch around the offending code",
          "set_exception_handler",
          "A finally block in the entry point"],
      c: 0,
      why: "A real fatal unwinds nothing and reaches no catch block. Without a shutdown function the log simply stops mid-request, which is the least debuggable failure mode there is: no error, no stack trace, just silence where the rest of the request should be." },

    { q: "Why should a rethrown exception always be given the original as its $previous?",
      a: ["Otherwise the original stack trace and cause are destroyed",
          "Otherwise PHP refuses to throw from inside a catch block",
          "Because $previous is required by the Throwable interface",
          "Because logging frameworks read $previous to decide severity"],
      c: 0,
      why: "An unchained rethrow reports where you caught the problem instead of where it happened. The chain is what turns 'order could not be placed' back into 'the payment gateway's TLS handshake timed out', which is the difference between a report and a diagnosis." },

    { q: "What is wrong with catching an exception and doing nothing in the catch block?",
      a: ["The failure becomes invisible while the code carries on in a state it did not plan for",
          "PHP emits a warning for empty catch blocks",
          "The exception is rethrown automatically at the end of the request",
          "Nothing, provided the block has a comment explaining why"],
      c: 0,
      why: "An empty catch converts a loud failure into a wrong answer delivered confidently. Either handle it, meaning do something that makes the situation right, or log it with context, or let it propagate. Swallowing it is the choice that guarantees a mystery later." },

    { q: "Where in an application should you catch Throwable rather than a specific exception type?",
      a: ["At the outermost boundary, such as the HTTP kernel or the queue worker loop",
          "In every service method, to keep failures local",
          "In the constructor of every entity",
          "Nowhere, because catching Throwable is always wrong"],
      c: 0,
      why: "The boundary is the one place that must never leak a stack trace to a user or lose a job silently, so it catches everything, logs it with a correlation id, and returns a clean error. Everywhere else, catching broadly hides bugs you could have fixed." },

    { q: "Why is defining a custom exception type such as InsufficientStockException worth the extra file?",
      a: ["It lets callers catch precisely that case and act on it, and it carries domain context",
          "It makes the exception faster to construct than a generic one",
          "It is required for the exception to appear in logs",
          "It allows PHP to retry the operation automatically"],
      c: 0,
      why: "Catching RuntimeException tells you nothing about what to do. A named type lets one caller show the customer a message and another put the order back on the queue, and it can carry the sku and the quantity that failed rather than a formatted sentence." },

    { q: "A request must call an API that takes ten seconds. What is the right answer in PHP?",
      a: ["Put the work on a queue and respond immediately",
          "Use fibers to run it concurrently with the rest of the request",
          "Increase max_execution_time and call it synchronously",
          "Fork a child process with pcntl_fork"],
      c: 0,
      why: "PHP is shared-nothing and request-scoped, and an FPM worker held for ten seconds is a worker serving nobody. Fibers give you cooperative concurrency inside one process, but they do not free the worker. The queue is the answer this market expects." },

    { q: "What do PHP fibers actually provide?",
      a: ["Cooperative multitasking inside one process, where code can suspend and resume",
          "Operating-system threads with shared memory",
          "Parallel execution across CPU cores",
          "Automatic background processing of slow functions"],
      c: 0,
      why: "A fiber is an interruptible call stack, which is the primitive async runtimes such as ReactPHP and Amp build on. It is not parallelism and it is not a queue, and offering it as the answer to 'how do you handle slow work' suggests the shared-nothing model has not landed yet." },

    { q: "What is the correct use of a finally block?",
      a: ["Releasing a resource so it happens whether or not an exception was thrown",
          "Returning the default value when the try block fails",
          "Logging the exception after catching it",
          "Retrying the failed operation"],
      c: 0,
      why: "finally is for cleanup that must happen on every path: closing a handle, releasing a lock, restoring a setting. Beware returning from finally, which discards an in-flight exception and produces a function that swallows failures without a catch block in sight." },
  ],

  /* ------------------------------------------------------- chapter 08 --- */
  "08-memory-and-references": [
    { q: "What does the shared-nothing model mean for a PHP request?",
      a: ["Nothing in memory survives the request, so state must live in Redis or a database",
          "Requests cannot share a database connection",
          "Each request runs in its own container",
          "Sessions are disabled unless explicitly enabled"],
      c: 0,
      why: "Every request starts with a clean slate and ends by throwing everything away. It is why PHP scales horizontally without effort, and why a static property is not a cache: it lives for one request in FPM, and suddenly for hours once the same code runs in a long-lived worker." },

    { q: "How does PHP normally decide that an object can be destroyed?",
      a: ["Refcounting, so destruction happens immediately when the last reference goes away",
          "A generational garbage collector running at intervals",
          "Only at the end of the request",
          "When the memory limit is approached"],
      c: 0,
      why: "Destruction is deterministic: refcount reaches zero and __destruct runs right there. This is why unset() on a large structure genuinely frees memory now. The exception is cycles, which refcounting cannot collect and the cycle collector handles later." },

    { q: "Two objects hold references to each other and nothing else points at either. What happens?",
      a: ["Refcounting cannot free them; the cycle collector must run to reclaim the memory",
          "They are freed immediately, because PHP tracks ownership",
          "They leak until the process exits, with no recovery",
          "PHP raises a fatal error on the circular reference"],
      c: 0,
      why: "Each keeps the other's refcount above zero, so neither ever reaches zero. PHP's cycle collector exists exactly for this and runs when the root buffer fills or on gc_collect_cycles(). It is the one place PHP's memory behaviour stops being immediate and predictable." },

    { q: "Why is &$object almost always unnecessary in PHP?",
      a: ["An object variable already holds a handle, so the callee sees the same object",
          "PHP ignores & on objects entirely",
          "Objects are immutable, so a reference changes nothing",
          "& only works on arrays and scalars"],
      c: 0,
      why: "Passing an object and mutating it inside a function already affects the caller. A reference on top of that lets the callee reassign the caller's variable to a different object, which is a much stranger power and almost never what was wanted." },

    { q: "What does clone do to an object whose property holds another object?",
      a: ["Copies the handle, so both the original and the clone point at the same inner object",
          "Recursively copies the whole object graph",
          "Throws unless __clone is defined",
          "Copies the inner object but not any array properties"],
      c: 0,
      why: "clone is shallow, in exactly the way readonly is shallow. If the copy must be independent, implement __clone and clone the inner objects there. Otherwise mutating the clone's collaborator quietly mutates the original's too." },

    { q: "Why is it advised never to store references inside an array?",
      a: ["Copying the array copies the references, so two arrays end up sharing elements",
          "PHP forbids it and raises a fatal error",
          "References in arrays cannot be serialised at all",
          "It doubles the memory used by the array"],
      c: 0,
      why: "The copy-on-write machinery does not break a reference, so an element that is a reference stays shared through every copy of the array. Writing through one array changes the other, which is a bug that survives being passed around by value and is very hard to see." },

    { q: "A script dies with 'Allowed memory size exhausted' while processing a report. What is the most likely cause?",
      a: ["It loaded an unbounded number of rows into memory instead of streaming them",
          "The memory limit was set too low for normal operation",
          "A memory leak in the PHP engine",
          "Too many objects were created without calling unset"],
      c: 0,
      why: "Nine times out of ten it is all() or fetchAll() over a table that grew. The fix is a generator, a cursor, or chunkById, not a larger memory_limit, because raising the limit only moves the failure to a later date and a larger dataset." },

    { q: "You move code from PHP-FPM into a long-running worker. Which construct most urgently needs re-reading?",
      a: ["Every static property and singleton, because their lifetime just changed from one request to hours",
          "Every database query, because connections behave differently",
          "Every include statement, because the autoloader changes",
          "Every closure, because bindTo behaves differently"],
      c: 0,
      why: "A static that cached one request's data is harmless in FPM and becomes a cross-tenant data leak in a worker that handles thousands of jobs. Long-lived runtimes such as Octane and RoadRunner make this the first audit to run, not the last." },

    { q: "In PHP-FPM, when does the memory used by a request actually get returned?",
      a: ["At the end of the request, when the worker tears down the request context",
          "Continuously, as the garbage collector runs during the request",
          "Only when the FPM pool is reloaded",
          "When the worker reaches pm.max_requests"],
      c: 0,
      why: "The request lifecycle is the unit of memory management: allocate during the request, release wholesale at the end. It is why a peak-memory spike inside one request matters even if the average is small, since memory_limit applies to the peak." },

    { q: "What does copy-on-write mean for the statement $b = $a where $a is a large array?",
      a: ["No copy happens until either variable is written to",
          "A full copy happens immediately",
          "Only the keys are copied, while values stay shared",
          "$b becomes a reference to $a"],
      c: 0,
      why: "The two names share one internal buffer with a refcount above one. The first write to either triggers the separation. This is why measuring memory before and after an assignment shows nothing, and why the spike appears on the line that modifies the copy." },
  ],

  /* ------------------------------------------------------ chapter 10b --- */
  "10b-views-and-procedures": [
    { q: "What is a MySQL view, precisely?",
      a: ["A stored SELECT statement that is expanded or materialised when you query it",
          "A physical copy of the underlying rows, refreshed on a schedule",
          "An index over several tables at once",
          "A permission wrapper with no effect on the query plan"],
      c: 0,
      why: "A view is a saved query, not saved data. MySQL either merges it into your outer query or materialises it into a temporary table, and which one it chooses is the difference between a fast view and a view that reads the whole table every time." },

    { q: "Why can filtering an aggregating view from outside still be slow?",
      a: ["The aggregation materialises first, so the filter runs after all rows have been computed",
          "Views ignore WHERE clauses applied externally",
          "MySQL disables indexes on any view",
          "The optimiser always chooses a full table scan for views"],
      c: 0,
      why: "MERGE cannot be used when the view aggregates, so MySQL builds the whole result into a temporary table and only then applies your predicate. The filter you thought would limit the work arrived too late to prevent any of it." },

    { q: "How do you get the effect of a materialised view in MySQL?",
      a: ["Maintain a summary table yourself and decide explicitly how stale it may be",
          "Use CREATE MATERIALIZED VIEW, which MySQL 8 supports",
          "Add WITH MATERIALIZED to the view definition",
          "Create a covering index over the view"],
      c: 0,
      why: "MySQL has no materialised views. What people call one is a summary table refreshed by a job or a trigger, which means you now own a staleness question, a refresh schedule and a backfill story. Those are the real costs to state out loud." },

    { q: "Why does wrapping an indexed column in a function inside a WHERE clause destroy performance?",
      a: ["The index stores the raw column value, so the engine must compute the function for every row",
          "Functions are not permitted in WHERE clauses on indexed columns",
          "The optimiser downgrades the query to a lower isolation level",
          "Function calls force a temporary table"],
      c: 0,
      why: "The B-tree is sorted by created_at, not by YEAR(created_at), so it cannot be searched for the transformed value. The predicate has become non-sargable. Rewrite it as a range, created_at >= and <, and the index is usable again." },

    { q: "What must a MySQL stored procedure include if a failure should not leave a transaction open?",
      a: ["A DECLARE EXIT HANDLER that rolls back and resignals",
          "A TRY/CATCH block around the body",
          "SET autocommit = 1 at the top",
          "Nothing, because MySQL rolls back automatically on error"],
      c: 0,
      why: "Without an exit handler the routine can abort partway with the transaction still open, holding locks until the connection is reused or dropped. It is the stored-procedure equivalent of a missing finally, and the symptom appears as unexplained lock waits." },

    { q: "Why are triggers usually considered the most dangerous of views, procedures and triggers?",
      a: ["They are invisible at the call site, run per row, and hide writes from the ORM",
          "They cannot be dropped once created",
          "They are the slowest of the three by design",
          "They cannot participate in a transaction"],
      c: 0,
      why: "Nothing in the application code says a trigger ran. Your ORM's model events did not fire, your audit log disagrees with reality, and a bulk update just executed business logic ten thousand times. Debugging starts with somebody remembering triggers exist." },

    { q: "You inherit a codebase with substantial logic in stored routines. What is the first thing to do?",
      a: ["Dump every routine definition into Git so the logic is versioned and reviewable",
          "Rewrite the routines in PHP immediately",
          "Drop the routines and reimplement on demand",
          "Add triggers so the routines fire automatically"],
      c: 0,
      why: "Logic that exists only inside a live database has no history, no review, and no diff. Getting the definitions into version control costs an afternoon and turns an opaque system into one you can reason about before you change anything." },

    { q: "Before moving a business rule out of a database trigger into application code, what must you establish?",
      a: ["Every other writer to that table, including imports, admin tools and other applications",
          "The trigger's average execution time",
          "Whether the table uses InnoDB or MyISAM",
          "Whether the ORM supports the same rule"],
      c: 0,
      why: "The trigger fired for everyone. Your application code fires only for your application. If a nightly CSV import or a legacy admin panel also writes to that table, moving the rule up silently exempts them from it, and the data drifts from that day onward." },

    { q: "What is a legitimate reason to keep a rule inside the database rather than the application?",
      a: ["Several independent applications write to the same tables and must all obey it",
          "The rule is complex and would need more PHP code",
          "It runs faster in SQL than in PHP",
          "It keeps the application layer thinner"],
      c: 0,
      why: "Multiple writers is the honest argument for database-enforced logic, and it is the same argument as a foreign key or a CHECK constraint. Speed is rarely it, and 'thinner application' just relocates complexity somewhere with worse tooling." },

    { q: "How does a stored function used in a WHERE clause affect a query over a large table?",
      a: ["It runs once per candidate row and prevents the index from being used",
          "It is evaluated once and its result cached for the query",
          "It is inlined by the optimiser like a view",
          "It forces the query to run on a replica"],
      c: 0,
      why: "It has the same non-sargable effect as any function on a column, with the added cost of a routine invocation per row. A query that looked declarative turns into a loop, and EXPLAIN shows the full scan that results." },
  ],

  /* ------------------------------------------------------- chapter 14 --- */
  "14-building-an-api": [
    { q: "What should validation at an API boundary produce?",
      a: ["A typed object the rest of the application can trust without re-checking",
          "A boolean saying whether the request was acceptable",
          "A sanitised array with dangerous characters stripped",
          "An exception only when the request is completely unparseable"],
      c: 0,
      why: "If validation returns an array, every layer downstream must keep asking whether a key exists and whether it is the right type. Validating into a typed object means the boundary is the last place that has to worry, which is the whole point of having one." },

    { q: "Why is Model::create($request->all()) dangerous even when the model has a fillable list?",
      a: ["It passes whatever the client sent, so the guard is the fillable list rather than your validation",
          "It bypasses the database transaction",
          "It skips model events",
          "It cannot be used with an auto-incrementing primary key"],
      c: 0,
      why: "all() is the raw input. You are relying on a second, easily forgotten mechanism to decide which fields count, and the day somebody adds a column without updating fillable, clients can write to it. validated() returns exactly the fields you defined rules for." },

    { q: "A request body parses correctly as JSON but the email field is not an email. Which status code is right?",
      a: ["422 Unprocessable Content",
          "400 Bad Request",
          "409 Conflict",
          "406 Not Acceptable"],
      c: 0,
      why: "400 means the server could not understand the request at all, such as malformed JSON. 422 means it was understood and is semantically wrong. Clients use the distinction to decide whether to show field errors or to report a bug in their own serialiser." },

    { q: "A client submits a form with five invalid fields. What should the API return?",
      a: ["All five errors at once, keyed by field",
          "The first error, so the client fixes them one at a time",
          "A single generic message, to avoid leaking the schema",
          "A 200 with a warnings array"],
      c: 0,
      why: "Returning one error at a time turns a single correction into five round trips and five moments of frustration. Field-keyed errors let the client mark every offending input at once, which is what any competent form UI needs." },

    { q: "Why should responses be built from a resource or DTO rather than by serialising the model directly?",
      a: ["The response is a public contract, and a model is an internal structure that will change",
          "Models cannot be converted to JSON without a custom method",
          "It is faster, because resources skip the ORM hydration step",
          "It is required for HTTP caching to work"],
      c: 0,
      why: "Serialising the model means a rename in the database is a breaking change for every client, and a new column is an accidental disclosure. A resource makes the contract explicit and lets the schema evolve underneath it." },

    { q: "How should a monetary amount be represented in a JSON API response?",
      a: ["As an integer in minor units, with the currency stated alongside",
          "As a float, which JSON handles natively",
          "As a formatted string such as EUR 12,50",
          "As a string in whatever locale the client requested"],
      c: 0,
      why: "Floats reintroduce binary rounding at the boundary, and a locale-formatted string forces every client to parse it back. Integer cents plus an explicit currency code is unambiguous, and formatting is a presentation decision the client should make." },

    { q: "Why does offset pagination degrade badly at page 4000?",
      a: ["The database must still read and discard every skipped row",
          "The offset value exceeds the maximum integer size",
          "The result set is cached and the cache expires",
          "Deep offsets force a full table lock"],
      c: 0,
      why: "OFFSET 80000 means fetch 80020 rows and throw 80000 away. Keyset pagination, asking for rows after the last id or timestamp you saw, is O(page size) at any depth and does not skip or duplicate rows when the data changes underneath." },

    { q: "When is offset pagination still the right choice?",
      a: ["When users genuinely need to jump to a numbered page in a stable, modest dataset",
          "When the endpoint must be cacheable",
          "When rows are inserted frequently at the head of the list",
          "When the client is a mobile application"],
      c: 0,
      why: "Keyset pagination cannot express 'go to page 7' without counting, so an admin table with page numbers over a few thousand stable rows is a legitimate use. Infinite-scroll feeds over growing data are exactly where it is the wrong tool." },

    { q: "Which change to an API response is safe for existing clients, and which is not?",
      a: ["Adding a field is safe; removing or renaming one is not",
          "Both are safe with a version header",
          "Removing is safe if the field was always null",
          "Neither is safe without a new major version"],
      c: 0,
      why: "A tolerant client ignores fields it does not know about, so additions are backwards compatible. Removals and renames break anybody reading that key, and the fact that a field was usually null does not mean nobody depended on its presence." },

    { q: "What does RFC 9457, Problem Details, standardise for an API?",
      a: ["A machine-readable JSON shape for error responses, with type, title, status and detail",
          "The authentication header format",
          "Rate limiting response headers",
          "The pagination link format"],
      c: 0,
      why: "Errors need a schema as much as successes do. A stable shape lets a client branch on a type URI rather than string-matching a human sentence, and adding a correlation id to it turns a user's screenshot into a log query." },
  ],

  /* ------------------------------------------------------ chapter 14b --- */
  "14b-integrations": [
    { q: "Which two timeouts must be set on every outbound HTTP call?",
      a: ["A connect timeout and a total or read timeout",
          "A DNS timeout and a TLS timeout",
          "A retry timeout and a backoff timeout",
          "A request timeout and a session timeout"],
      c: 0,
      why: "Connecting can hang and so can waiting for a response, and they fail for different reasons. With neither set, a partner's outage becomes your outage: FPM workers pile up waiting on a socket, and the site goes down for a dependency nobody thought was critical." },

    { q: "Which HTTP responses are worth retrying automatically?",
      a: ["429 and 5xx, with exponential backoff and jitter",
          "Any response that is not 200",
          "4xx responses, since the request may have been malformed in transit",
          "Only 503, since it explicitly means try again"],
      c: 0,
      why: "A 4xx means you sent something wrong, and repeating it produces the same answer while adding load. 429 and 5xx are the retryable cases. Jitter matters because synchronised retries from many workers recreate the spike that caused the failure." },

    { q: "What makes retrying a POST safe?",
      a: ["An idempotency key the receiving system uses to deduplicate",
          "Setting the Retry-After header on the request",
          "Wrapping the call in a database transaction",
          "Nothing; POST can always be retried safely"],
      c: 0,
      why: "The dangerous case is the response getting lost after the far end succeeded, which your code cannot distinguish from never arriving. With an idempotency key the receiver recognises the repeat and returns the original result. Without one, retrying is a business decision about double charges." },

    { q: "How should an incoming webhook signature be verified?",
      a: ["Over the exact raw body, compared with hash_equals",
          "Over the decoded and re-encoded JSON, compared with ===",
          "By checking the sender's IP address against an allowlist",
          "By confirming the payload contains a known event id"],
      c: 0,
      why: "Re-encoding changes bytes, such as key order or unicode escaping, so the HMAC no longer matches. And === on secrets short-circuits on the first differing byte, which leaks timing information. Raw body plus hash_equals is the pair." },

    { q: "Webhook delivery is at-least-once. What is the most robust way to handle duplicates?",
      a: ["A unique constraint on the provider's event id and letting the database reject the repeat",
          "Checking for an existing row before inserting",
          "Keeping recently seen ids in a static array",
          "Trusting the provider not to send duplicates"],
      c: 0,
      why: "Check-then-insert has a race that two concurrent deliveries will find. A unique index makes the database arbitrate, and catching the duplicate-key error is the whole deduplication logic. It is the same reasoning as the idempotent checkout." },

    { q: "What is the right shape for a webhook endpoint that must do real work?",
      a: ["Store the payload, respond 200 immediately, process it on a queue",
          "Process it synchronously and respond when finished",
          "Respond 202 and process it in a shutdown function",
          "Respond 200 and discard anything that takes too long"],
      c: 0,
      why: "Providers time out and retry, so slow processing multiplies deliveries. Storing then queueing keeps the response fast, survives a processing bug because the payload is still there, and gives you replay for free when you fix the handler." },

    { q: "What is the XXE risk when parsing untrusted XML in PHP?",
      a: ["External entity declarations can read local files or make network requests from your server",
          "Deeply nested XML exhausts the memory limit",
          "Invalid encoding declarations corrupt the parsed values",
          "Namespaces can collide and overwrite parsed nodes"],
      c: 0,
      why: "A crafted DOCTYPE can pull in /etc/passwd or hit an internal URL, turning your parser into a file reader and an SSRF tool. Modern libxml disables external entity loading by default, but the safe habit is to disable it explicitly and reject DOCTYPEs outright." },

    { q: "Which three problems should you expect on every CSV integration?",
      a: ["Character encoding, partially written files, and the comma used as a decimal separator",
          "Compression, encryption, and checksums",
          "Row limits, column limits, and header casing",
          "Time zones, currencies, and languages"],
      c: 0,
      why: "Italian exports arrive in Windows-1252 with 12,50 meaning twelve euros fifty, and FTP hands you a file that is still being written. Detect the encoding, transfer to a temporary name and rename on completion, and parse decimals with the source's locale in mind." },

    { q: "You must integrate with a SOAP service. What is the pragmatic PHP approach?",
      a: ["Use the SoapClient extension against the WSDL, then wrap it behind your own interface",
          "Hand-build the XML envelopes with string concatenation",
          "Convert the WSDL to REST with a proxy service",
          "Refuse and require the partner to expose JSON"],
      c: 0,
      why: "SOAP is unavoidable when integrating with banks, couriers and public administration in this market. SoapClient does the envelope work; wrapping it in your own interface keeps the vendor's shape out of your domain and gives you a seam to fake in tests." },

    { q: "A partner's FTP drop occasionally contains a file with only half its rows. What is the standard fix?",
      a: ["Ask them to upload to a temporary name and rename it once complete",
          "Retry reading the file until the row count stabilises",
          "Validate the row count against a header line",
          "Poll less frequently so uploads finish first"],
      c: 0,
      why: "Rename is atomic on the same filesystem, so a file only appears under its real name when it is whole. Sleeping and hoping is the workaround people reach for first, and it fails on the day the upload is slower than usual." },
  ],

  /* ------------------------------------------------------ chapter 16b --- */
  "16b-legacy-php": [
    { q: "What is the correct order of operations in your first week on an undocumented legacy PHP application?",
      a: ["Get it running locally, then into Git, then add tests, then change things",
          "Change the most obvious bug first to build credibility",
          "Upgrade PHP to a supported version immediately",
          "Rewrite the worst module while learning the system"],
      c: 0,
      why: "Each step makes the next one safe. Without a local copy you cannot experiment, without history you cannot undo, and without tests you cannot tell whether your change altered behaviour. Reversing the order is how a first week produces an outage." },

    { q: "What is a characterisation test?",
      a: ["A test that records what the code currently does, including its bugs",
          "A test written from the original specification",
          "A performance test capturing current response times",
          "A test that describes the intended future behaviour"],
      c: 0,
      why: "You cannot write correctness tests for behaviour nobody can describe. Pin down what it does today, bugs included, and you gain a safety net for refactoring. Fixing a bug then becomes a deliberate act with a changed assertion, not an accident." },

    { q: "The legacy application is genuinely bad. Why is proposing a rewrite usually the wrong move?",
      a: ["It discards years of encoded edge cases and stops delivery for months with no interim value",
          "Rewrites are technically impossible in PHP",
          "Management never funds rewrites",
          "The old code cannot be deleted for legal reasons"],
      c: 0,
      why: "Much of what looks like mess is accumulated knowledge about real cases. The strangler pattern routes one path at a time to new code behind the same URL, so value ships continuously and the risky parts are approached with the lessons of the easy ones." },

    { q: "You must move an application from PHP 5.6 to 8.2. What is the safest path?",
      a: ["One major version at a time, with Rector automating the mechanical changes",
          "Jump straight to 8.2, since the intermediate versions are unsupported anyway",
          "Rewrite each file by hand for full control",
          "Run 5.6 and 8.2 side by side indefinitely"],
      c: 0,
      why: "Each major version has its own breaking changes, and stepping through them keeps every failure attributable. Rector applies the repetitive transformations reliably, which leaves your attention for the genuinely ambiguous cases it flags." },

    { q: "What does a PHPStan baseline give you on a legacy codebase?",
      a: ["Existing violations are frozen and ignored, while new code must pass",
          "The analysis level is lowered until the code is clean",
          "Errors are downgraded to warnings across the project",
          "The tool auto-fixes the existing violations"],
      c: 0,
      why: "Without a baseline, ten thousand pre-existing errors mean the tool is switched off within a week. Freezing them turns static analysis into a ratchet: the debt cannot grow, and it shrinks whenever somebody touches a file for other reasons." },

    { q: "What does 'sprouting' mean when adding a feature to tangled legacy code?",
      a: ["Writing the new logic in a fresh, tested class and calling it from the old code",
          "Copying the old function and editing the copy",
          "Adding the feature behind a feature flag in the same function",
          "Extracting the whole file into a new module first"],
      c: 0,
      why: "Untangling first is a large, risky, unfunded refactor. A sprouted class is new code you can test properly, and the legacy function gains one call. Over time the sprouts become the new system, which is the strangler pattern at method scale." },

    { q: "Why should a refactor and a behaviour change never share a commit?",
      a: ["A reviewer cannot tell which line changed behaviour, and a bisect cannot isolate it",
          "Git cannot merge commits containing both",
          "Continuous integration rejects mixed commits",
          "It makes the diff larger than the review tool can display"],
      c: 0,
      why: "A five-hundred-line diff that both moves code and changes a rule is reviewed as 'looks like a refactor' and the rule change goes unexamined. Two commits make both halves obvious and make git bisect useful when something breaks a month later." },

    { q: "The legacy code has no dependency injection and calls a static Database class everywhere. How do you begin testing a function that uses it?",
      a: ["Introduce a seam, such as passing the dependency in or extracting the call to an overridable method",
          "Mock static calls with a runkit-style extension in production tests",
          "Rewrite the whole data layer before writing any test",
          "Test only against the production database"],
      c: 0,
      why: "A seam is any place you can change behaviour without editing the code under test. Adding an optional constructor parameter that defaults to the old static call is a one-line change that makes the function testable without touching its callers." },

    { q: "You find a function used in forty places, and it has a bug. What does the strangler approach suggest?",
      a: ["Write the correct version alongside, migrate callers deliberately, then delete the old one",
          "Fix it in place and accept whatever breaks",
          "Leave it and document the bug",
          "Fix it in place but keep the old behaviour behind a flag forever"],
      c: 0,
      why: "Forty callers may depend on the current behaviour, bug included. A parallel correct implementation lets you migrate them in reviewable steps with tests, rather than making one change whose blast radius nobody can assess." },

    { q: "Why is getting a legacy application running locally often the hardest step, and worth the effort anyway?",
      a: ["It usually depends on undocumented server state, and without it every change is tested in production",
          "It is only needed if you plan to refactor",
          "Local environments always differ enough to be misleading",
          "It matters less once tests exist"],
      c: 0,
      why: "The hidden cron job, the hard-coded path, the extension nobody mentioned: reproducing the environment is how you discover them, and each discovery is documentation. Until it runs locally, the feedback loop for every change is a deploy." },
  ],

  /* ------------------------------------------------------- chapter 17 --- */
  "17-frontend-basics": [
    { q: "Why does using a div with a click handler instead of a button matter?",
      a: ["The button carries keyboard focus, Enter and Space activation, and a role assistive technology understands",
          "The div is slower to render",
          "The div cannot receive CSS styling for hover states",
          "The button is required for the form to submit over HTTPS"],
      c: 0,
      why: "The tag is a contract with the browser and with assistive technology. Recreating focus, keyboard activation and announced role on a div is a lot of code to arrive back at what button already does, and it is usually incomplete." },

    { q: "How is CSS specificity calculated?",
      a: ["As a tuple of ids, classes and elements, compared left to right, with source order breaking ties",
          "By counting total selector characters",
          "By the position of the rule in the stylesheet only",
          "By the nesting depth of the selector"],
      c: 0,
      why: "One id beats any number of classes, because the comparison is positional rather than additive. Knowing this turns 'why is my rule not applying' from guesswork into arithmetic, and it explains why an inline style beats almost everything." },

    { q: "What do :where() and @layer offer instead of reaching for !important?",
      a: ["Zero-specificity defaults and explicit cascade ordering, so later rules win predictably",
          "A way to raise specificity above ids",
          "Automatic vendor prefixing",
          "Scoped styles that cannot leak out of a component"],
      c: 0,
      why: "!important starts a war that ends with !important everywhere. :where() lets a library ship defaults that any author rule overrides, and @layer lets you declare that your styles come after the framework's regardless of selector strength." },

    { q: "A mysterious vertical gap appears between two blocks and no element seems to have that margin. What is the likely cause?",
      a: ["Collapsing margins between adjacent or nested block elements",
          "A stray non-breaking space in the markup",
          "The default line-height of the parent",
          "A rounding error in the browser's layout engine"],
      c: 0,
      why: "Adjacent vertical margins collapse to the larger of the two, and a parent can collapse with its first or last child. It is vertical only, and it does not happen in flex or grid containers, which is why the bug tends to disappear the moment you add display: flex." },

    { q: "What is the one-sentence rule for choosing between flexbox and grid?",
      a: ["Flex lays out one dimension, grid lays out two",
          "Flex is for text, grid is for images",
          "Grid is for page layout, flex is deprecated",
          "Flex is for mobile, grid is for desktop"],
      c: 0,
      why: "A row of buttons or a navbar is one dimension and belongs to flex. A card layout with aligned rows and columns is two and belongs to grid. They compose freely, and choosing on that basis avoids most fights with either." },

    { q: "A flex item containing long text refuses to shrink and overflows its container. What is the usual fix?",
      a: ["Set min-width: 0 on the flex item",
          "Set overflow: hidden on the container",
          "Set flex-shrink: 0 on the item",
          "Set width: 100% on the item"],
      c: 0,
      why: "A flex item's default min-width is auto, meaning it will not shrink below its content's intrinsic size. min-width: 0 releases that floor, which is the answer to a very large share of 'my flex layout overflows' questions." },

    { q: "Which CSS grid declaration produces a responsive column layout with no media queries?",
      a: ["repeat(auto-fill, minmax(220px, 1fr)) with a gap",
          "grid-template-columns: 25% 25% 25% 25%",
          "display: grid with float fallbacks",
          "grid-auto-flow: column with overflow scrolling"],
      c: 0,
      why: "auto-fill with minmax lets the browser decide how many columns fit at the current width, and 1fr shares the remainder. It is the layout that adapts to its container rather than to guessed device widths, which is what container-aware design means in practice." },

    { q: "What is the fastest meaningful accessibility check on a page you have just built?",
      a: ["Tab through it and confirm focus is visible, ordered, and every control is reachable",
          "Run an automated audit and fix only the errors it reports",
          "Confirm every image has an alt attribute",
          "Check that the page validates as HTML5"],
      c: 0,
      why: "Keyboard traversal exposes the structural failures that matter: unreachable controls, invisible focus, a modal that does not trap focus. Automated tools catch perhaps a third of real issues, and alt text alone is a small slice of that third." },

    { q: "In Italy, why is web accessibility often a contractual matter rather than a nicety?",
      a: ["Public sector work is bound by accessibility law, so suppliers inherit the obligation",
          "Hosting providers require it for TLS certification",
          "Search engines refuse to index inaccessible sites",
          "It is required by the GDPR"],
      c: 0,
      why: "Legge Stanca and the EU directives make accessibility a requirement on public administration projects, and agencies serving them pass that requirement down. Knowing it is a compliance question, not a preference, changes how you argue for the time." },

    { q: "What does semantic markup give you beyond styling?",
      a: ["Meaning that browsers, assistive technology and search engines can act on",
          "Smaller HTML payloads",
          "Automatic responsive behaviour",
          "Protection against cross-site scripting"],
      c: 0,
      why: "A nav, a main and a heading hierarchy let a screen-reader user jump around the page, let the browser offer reader mode, and let a crawler understand structure. A page of divs looks identical and communicates nothing." },
  ],

  /* ------------------------------------------------------ chapter 17b --- */
  "17b-javascript": [
    { q: "A promise callback and a setTimeout with zero delay are both scheduled. Which runs first?",
      a: ["The promise callback, because microtasks drain before the next macrotask",
          "The setTimeout, because zero delay means immediately",
          "Whichever was scheduled first",
          "They run concurrently on separate threads"],
      c: 0,
      why: "The event loop empties the entire microtask queue after each task, and promise callbacks are microtasks while timers are macrotasks. This is why a chain of .then calls completes before a setTimeout(0) that was registered earlier." },

    { q: "What does it mean that JavaScript in the browser is single-threaded?",
      a: ["A long synchronous loop blocks rendering and input entirely",
          "Only one script file can execute per page",
          "Asynchronous callbacks cannot be used",
          "Web workers are unavailable"],
      c: 0,
      why: "The page is not slow during a two-second loop, it is frozen: no repaint, no scroll, no clicks. Recognising blocked rather than slow points you at chunking the work or moving it to a web worker rather than at micro-optimising it." },

    { q: "What is wrong with await inside a for loop over independent requests?",
      a: ["It runs them one after another, so total time is the sum rather than the maximum",
          "It silently drops all but the last result",
          "It throws when the loop body is asynchronous",
          "It leaks memory, because each promise is retained"],
      c: 0,
      why: "await pauses the loop until that request finishes. When the calls do not depend on each other, start them all and await Promise.all, which turns ten sequential 200 ms calls into one 200 ms wait. Sequential await is right only when each call needs the previous result." },

    { q: "Why does an async callback passed to Array.prototype.forEach not work as expected?",
      a: ["forEach ignores the returned promise, so it does not wait for anything",
          "forEach cannot accept a function declared async",
          "The callback runs but its return value overwrites the array",
          "It works correctly; the problem is only with map"],
      c: 0,
      why: "forEach calls the function and discards the result, so all the promises start and the loop finishes immediately with nothing awaited. Use a for...of loop with await for sequential work, or map plus Promise.all for parallel." },

    { q: "A fetch() call receives a 500 response. What does the promise do?",
      a: ["It resolves, and res.ok is false",
          "It rejects with an HTTP error",
          "It resolves only after the retry limit is reached",
          "It rejects only for 5xx, not 4xx"],
      c: 0,
      why: "fetch rejects for network-level failures, not for HTTP status codes: a 404 or 500 is a successfully completed exchange. Forgetting to check res.ok means error pages get parsed as if they were data, which is how a JSON parse error becomes the reported bug." },

    { q: "Why are arrow functions the right choice for callbacks that reference this?",
      a: ["An arrow function has no this of its own, so this stays lexically bound to the surrounding scope",
          "Arrow functions bind this to the global object",
          "Arrow functions bind this to the callback's caller",
          "Arrow functions cache this at the first invocation"],
      c: 0,
      why: "A regular function's this depends on how it is called, which is why this.setState inside a setTimeout callback used to be undefined. The arrow function's lexical this removes an entire family of bugs that bind and const self = this were invented to work around." },

    { q: "What problem did let solve in a for loop that registers callbacks?",
      a: ["Each iteration gets a fresh binding, so callbacks capture the value from their own iteration",
          "It prevents the loop variable from being reassigned",
          "It makes the loop run asynchronously",
          "It hoists the variable to the top of the function"],
      c: 0,
      why: "With var there is one function-scoped binding, so every callback sees the final value. let creates a new binding per iteration, which is exactly what the closure was assumed to capture. It is the classic interview question about closures in loops." },

    { q: "When should ?? be preferred over || for a default value?",
      a: ["When 0 or an empty string are legitimate values that must not be replaced",
          "When the left side may be an object",
          "When the default is a function call",
          "Never, since they behave identically"],
      c: 0,
      why: "|| replaces any falsy value, so a quantity of 0 or a deliberately empty note becomes the default. ?? substitutes only for null or undefined, which is what 'no value was provided' actually means." },

    { q: "What does the spread of an object, such as { ...a, ...b }, do on key collisions?",
      a: ["The later spread wins",
          "The earlier spread wins",
          "It throws on duplicate keys",
          "The values are merged recursively"],
      c: 0,
      why: "Later wins, which is the opposite of the PHP + operator on arrays and the same as PHP's array_merge. It is also shallow, so a nested object is shared between the source and the result rather than copied." },

    { q: "What does the event loop do after finishing a task such as a click handler?",
      a: ["Drains all pending microtasks, then may render, then takes the next task",
          "Renders immediately, then takes the next task",
          "Takes the next task, deferring microtasks until the queue is idle",
          "Runs timers first, then microtasks"],
      c: 0,
      why: "Task, then the whole microtask queue, then an opportunity to paint. This ordering explains both why a promise chain can starve rendering if it never yields, and why a DOM change followed by a promise callback is not yet visible on screen." },
  ],

  /* ------------------------------------------------------- chapter 18 --- */
  "18-spa-and-vue": [
    { q: "In Vue's reactivity system, what determines which components re-render when a value changes?",
      a: ["The tracked dependencies, meaning what each render actually read",
          "A deep comparison of the previous and current state trees",
          "Explicit subscriptions declared by the developer",
          "The component hierarchy, since all descendants re-render"],
      c: 0,
      why: "Reactivity records the properties read during render and re-runs only the effects that depend on them. It is why a change to an unused property costs nothing, and why destructuring a reactive object breaks updates: you took the value and left the tracking behind." },

    { q: "Why does destructuring a reactive() object lose reactivity?",
      a: ["Destructuring copies the current values, detaching them from the proxy that tracks access",
          "Destructuring is not supported inside setup()",
          "The proxy is recreated on every render",
          "It only breaks for nested properties"],
      c: 0,
      why: "The proxy intercepts property access on the object, and destructuring performs that access once, keeping only the result. toRefs() exists precisely to destructure while preserving the connection, and it is one of the first things a Vue interview probes." },

    { q: "When should you use computed rather than watch?",
      a: ["Whenever the goal is to derive a value from other state",
          "Whenever the derivation involves an asynchronous call",
          "Whenever more than one component reads the result",
          "Whenever the source value changes frequently"],
      c: 0,
      why: "computed is declarative, cached, and cannot drift out of sync. A watch that sets another piece of state is a missed computed, and it introduces an intermediate moment where the two values disagree, which is where the bug reports come from." },

    { q: "Why should the array index never be used as a :key in a list that can reorder?",
      a: ["The key must identify the item, or Vue reuses the wrong DOM node and its local state",
          "Indexes are strings and keys must be numbers",
          "Vue throws a runtime error for index keys",
          "It only matters for performance, not correctness"],
      c: 0,
      why: "Delete the first row and every subsequent item's index shifts, so Vue believes item two is now item one and keeps the old DOM, including a half-typed input or an open dropdown. A stable id keeps identity attached to the data it belongs to." },

    { q: "What is the correct data flow between a parent and a child component?",
      a: ["Props down, events up, with the child never mutating a prop",
          "Two-way binding on every prop, so both stay in sync",
          "A shared global store, avoiding props entirely",
          "The child mutates the prop and the parent watches it"],
      c: 0,
      why: "One-way flow means you can always answer 'who changed this' by looking at the owner. A child that mutates a prop makes the parent's state change from a place the parent cannot see, which is the hardest kind of bug to trace in a component tree." },

    { q: "Why should server data be kept separate from client application state?",
      a: ["Server data is a cache with staleness and refetch concerns that global state does not model",
          "Server data cannot be stored in a reactive object",
          "It is required for server-side rendering to work",
          "Mixing them prevents TypeScript from inferring types"],
      c: 0,
      why: "Copying API responses into a global store means hand-writing caching, invalidation, loading and error states for every endpoint. Tools like TanStack Query exist because that is a solved problem, and conflating it with genuine UI state makes both harder." },

    { q: "You changed a reactive value and want to measure the resulting DOM. What must you do first?",
      a: ["Await nextTick, because DOM updates are batched and applied asynchronously",
          "Call forceUpdate on the component",
          "Read the value once to trigger the update",
          "Nothing, because DOM updates are synchronous"],
      c: 0,
      why: "Vue queues updates and flushes them once per tick, which is what makes many changes cheap. Reading offsetHeight immediately after a data change gives you the old layout, and nextTick is the documented point at which the DOM matches the state." },

    { q: "What is the strongest way to keep TypeScript types on the client honest about the API?",
      a: ["Generate them from the API's schema, so a backend change breaks the build",
          "Hand-write interfaces and review them at each release",
          "Use any at the boundary and cast where needed",
          "Validate at runtime and infer the types from the validation"],
      c: 0,
      why: "Hand-written types describe a shape nothing verifies: rename a field on the server and TypeScript keeps confidently reporting the old one. Generating from OpenAPI or a schema makes the compiler an actual integration test." },

    { q: "A gestionale needs CRUD screens over forty tables. What does that argue for?",
      a: ["Server-rendered pages, because an SPA adds an API boundary the requirements do not need",
          "A Vue SPA, because forty tables need client-side routing",
          "A separate SPA per module",
          "Static site generation with a headless backend"],
      c: 0,
      why: "An SPA buys rich interactivity at the cost of a second application, an API contract and a duplicated validation story. Forms and tables do not need that. Livewire or Inertia give interactivity where it is genuinely wanted without the split." },

    { q: "What is the actual purpose of a key on a component in a list?",
      a: ["To give the item a stable identity across renders so state and DOM follow the right data",
          "To improve rendering performance by skipping the diff",
          "To allow the component to be referenced from the parent",
          "To ensure the list is sorted consistently"],
      c: 0,
      why: "It is about identity, not speed. The diffing algorithm uses it to decide what moved, what is new and what is gone, and getting it wrong shows up as component state attached to the wrong row rather than as a slow page." },
  ],

  /* ------------------------------------------------------- chapter 19 --- */
  "19-server-rendered-ui": [
    { q: "In Blade, what is the difference between the double-brace and the exclamation-mark echo?",
      a: ["The double brace escapes HTML; the exclamation form outputs raw and must be audited",
          "The exclamation form escapes more aggressively",
          "The double brace is for variables, the exclamation form for expressions",
          "They are identical, with the second kept for compatibility"],
      c: 0,
      why: "The raw form is the one place in a Blade template where user data can become markup. Grepping for it is a genuine security review step, and every occurrence should have a reason next to it explaining why the content is trusted." },

    { q: "Why is HTML escaping insufficient when placing a PHP value inside a script tag?",
      a: ["The JavaScript context has different dangerous characters, so a value can break out of the string",
          "Script tags ignore HTML entities entirely",
          "Escaping corrupts numeric values",
          "It is sufficient, provided the value is quoted"],
      c: 0,
      why: "HTML escaping targets angle brackets and ampersands, while a JavaScript string is escaped by breaking out of quotes or ending the script tag. Blade's json directive encodes the value safely for that context, which is why context-aware escaping is the rule." },

    { q: "A user-supplied URL is HTML-escaped and placed in an href. What attack remains possible?",
      a: ["A javascript: scheme URL, which survives escaping and executes on click",
          "Header injection through the URL fragment",
          "SQL injection through the query string",
          "None, since escaping neutralises the value"],
      c: 0,
      why: "Escaping protects the surrounding markup, not the semantics of the attribute. javascript:alert(1) contains no characters escaping would touch. Any user-supplied URL needs its scheme validated against an allowlist such as http and https." },

    { q: "Where does an N+1 query hide in a server-rendered application?",
      a: ["In a view that touches a lazy relation inside a loop, which the controller never shows",
          "In a controller that eager-loads too many relations",
          "In the router, when resolving nested resources",
          "In middleware that queries per request"],
      c: 0,
      why: "The controller looks clean, and the query only appears when the template dereferences a relation for each row. It is why Laravel's preventLazyLoading in development is so useful: it makes the failure loud at exactly the point it is created." },

    { q: "What is the essential trade-off Livewire makes?",
      a: ["Interactivity without leaving PHP, at the cost of a network round trip per interaction",
          "Client-side rendering with no server involvement after load",
          "Compiled JavaScript output from PHP source",
          "Real-time updates over a persistent WebSocket by default"],
      c: 0,
      why: "Each action posts the component state to the server and swaps in the rendered HTML. That is a good trade for admin panels and gestionali on a fast network, and a poor one for a drag-and-drop canvas or anything expected to work offline." },

    { q: "Why should a Livewire text input use a debounced live binding rather than a plain one?",
      a: ["Without debouncing, every keystroke sends a request to the server",
          "Plain binding does not update the server at all",
          "Debouncing is required for validation to run",
          "It prevents the component state from being exposed"],
      c: 0,
      why: "A search box with an undebounced live binding produces a request per character, which multiplies both server load and visible lag. Debouncing waits for a pause in typing, which is what the user meant by 'search as I type' anyway." },

    { q: "What must you never place in a Livewire public property?",
      a: ["Secrets or large data, since public properties are serialised to the browser and back",
          "Any array, since arrays cannot be serialised",
          "Values that change during the request",
          "Anything typed as a string"],
      c: 0,
      why: "Public properties are the component's state, and state travels to the client. A hidden price, an internal id list, or an entire collection all become visible and tamperable, and the payload size is paid on every interaction." },

    { q: "What does Inertia give you that a traditional API plus SPA does not?",
      a: ["Real Vue or React pages with server-side routing and controllers, and no separate API to maintain",
          "Server-side rendering of arbitrary PHP components",
          "Automatic offline support",
          "A GraphQL layer over the models"],
      c: 0,
      why: "Inertia keeps the server as the router and the source of data while the pages are genuine client components. You lose the general-purpose API, which matters only if somebody else consumes it, and you avoid duplicating routing, auth and validation." },

    { q: "When is building a separate JSON API genuinely justified?",
      a: ["When a consumer other than your own web pages needs the data",
          "Whenever the interface requires client-side interactivity",
          "Whenever the team uses Vue",
          "Whenever the application has more than one page"],
      c: 0,
      why: "An API is a contract with an audience: a mobile app, a partner, a second frontend. Building one only to make your own web page interactive means maintaining a public-shaped boundary for a single private consumer, and paying for it forever." },

    { q: "In Twig or Blade, what is template inheritance for?",
      a: ["Defining a layout with named blocks that child templates fill in",
          "Sharing PHP classes between templates",
          "Allowing a template to extend a controller",
          "Compiling templates into a single cached file"],
      c: 0,
      why: "One layout holds the page shell and declares the holes; each page fills them. Components and includes cover reuse of fragments, and knowing which of the three you want is most of the skill of keeping a template directory navigable." },
  ],

  /* ------------------------------------------------------- chapter 20 --- */
  "20-caching": [
    { q: "What is the correct order of operations when a page is slow?",
      a: ["Measure, fix the query, and only then consider caching",
          "Cache first, then measure the improvement",
          "Add an index, then cache, then measure",
          "Increase the memory limit, then cache"],
      c: 0,
      why: "Caching a slow query hides it: the cost returns on every miss, on every deploy that clears the cache, and on the first cold morning. Fixing the query lowers the floor for everyone, and whatever caching you add afterwards is then genuinely optional." },

    { q: "What must a cache key contain?",
      a: ["Everything that changes the answer, such as tenant, locale and any version marker",
          "Only the primary identifier of the record",
          "A hash of the rendered output",
          "The current timestamp, to guarantee freshness"],
      c: 0,
      why: "A key missing the tenant serves one customer's data to another, and a key missing the locale serves Italian to an English user. The failure is not a stale page, it is a wrong page, and in a multi-tenant gestionale that is a serious incident." },

    { q: "How should a TTL be chosen?",
      a: ["As a business decision about how stale the data may acceptably be",
          "As the average time between writes",
          "As long as possible, to maximise the hit rate",
          "As short as possible, to guarantee correctness"],
      c: 0,
      why: "The question is how out of date this may be before somebody is misled or harmed. A product listing can be five minutes stale; an available-stock figure during a flash sale cannot. Framing it that way makes the answer a conversation rather than a guess." },

    { q: "Why is key versioning often better than explicit cache deletion?",
      a: ["There is no delete to forget, since a new version simply misses and the old entries expire",
          "Deletion is not supported by Redis clusters",
          "Versioned keys use less memory",
          "Deletion invalidates the entire cache"],
      c: 0,
      why: "Every deletion is a line of code somebody must remember to write in every place that writes. Bumping a version in the key means the write path changes one value and all derived keys become unreachable at once, expiring quietly on their own." },

    { q: "A popular cache key expires and hundreds of requests miss at the same moment. What is this called and how is it handled?",
      a: ["A cache stampede, handled by locking the recompute or serving stale while one worker refreshes",
          "A cache avalanche, handled by increasing the TTL",
          "Cache poisoning, handled by validating the key",
          "A thundering write, handled by write-behind caching"],
      c: 0,
      why: "Every one of those requests runs the expensive query simultaneously, often overwhelming the database that the cache existed to protect. A lock so only one recomputes, or serving the stale value while it refreshes, keeps the load flat." },

    { q: "Why should TTLs be jittered?",
      a: ["Keys warmed together would otherwise expire together and produce a synchronised miss storm",
          "Redis rejects identical TTLs on many keys",
          "Jitter improves the hash distribution across the cluster",
          "It reduces memory fragmentation"],
      c: 0,
      why: "A deploy warms a thousand keys within a second, and with a flat one-hour TTL they all expire within the same second an hour later. Adding a random spread turns one spike into a smooth trickle of individual refreshes." },

    { q: "Why should the cache and the queue not share a Redis instance?",
      a: ["Cache eviction under memory pressure can delete queued jobs",
          "They require incompatible Redis versions",
          "The queue needs persistence that the cache cannot use",
          "Sharing prevents the use of Redis transactions"],
      c: 0,
      why: "A cache is configured to evict when memory fills, which is correct for a cache and catastrophic for a job that represents work someone is waiting for. Different data with different durability requirements deserves different instances or at least different databases." },

    { q: "Which HTTP cache directive must be used for a response containing user-specific data?",
      a: ["private, so shared caches and CDNs do not store it",
          "public with a short max-age",
          "no-transform",
          "must-revalidate on its own"],
      c: 0,
      why: "A CDN takes public literally and will happily serve one user's dashboard to the next visitor. private restricts storage to the end user's own browser. Getting this wrong is one of the highest-impact single-word mistakes in web development." },

    { q: "What is the cache-aside pattern?",
      a: ["Read the cache, and on a miss compute the value, store it, and return it",
          "Write to the cache and let it write through to the database",
          "Keep the cache updated by a background job only",
          "Bypass the cache for writes and invalidate on read"],
      c: 0,
      why: "It is the default because it is simple and degrades safely: if the cache is down, everything still works and is merely slower. Write-through and write-behind couple the application to the cache's availability in exchange for freshness." },

    { q: "What does OPcache actually cache?",
      a: ["Compiled PHP bytecode, so source files are not reparsed on every request",
          "The rendered HTML output of each page",
          "Database query results",
          "Composer's autoload map"],
      c: 0,
      why: "It removes the parse and compile step from every request, which is a large win for free. It also means a deploy that changes files needs an FPM reload when validate_timestamps is off, or the old bytecode keeps serving the old code." },
  ],

  /* ------------------------------------------------------- chapter 21 --- */
  "21-search": [
    { q: "Why does a LIKE query with a leading wildcard fail twice over?",
      a: ["It cannot use a B-tree index, and it has no notion of relevance ranking",
          "It is case-sensitive and cannot be made otherwise",
          "It matches only whole words",
          "It is rejected by MySQL on large tables"],
      c: 0,
      why: "The index is sorted by prefix, so a leading wildcard forces a full scan. The worse half is quality: a substring match cannot rank results, handle a misspelling, or know that scarpe and scarpa are the same word. Users experience the second problem first." },

    { q: "What is an inverted index?",
      a: ["A map from each term to the documents containing it",
          "An index stored in reverse sort order",
          "A B-tree built on the reversed string",
          "A cache of previous search results"],
      c: 0,
      why: "It is the same structure as the index at the back of a book, and it is why full-text search is fast: finding every document containing a word is a lookup rather than a scan. Everything else in a search engine is built on top of that idea." },

    { q: "Why must the analyser be identical at index time and at query time?",
      a: ["Otherwise the query's tokens do not match the stored tokens and results silently disappear",
          "Otherwise the index becomes corrupted",
          "Otherwise the engine cannot compute relevance scores",
          "It need not be, since the engine reconciles them automatically"],
      c: 0,
      why: "If indexing folds accents and the query does not, perche never matches perché. Nothing errors; the result set is just empty or wrong. For Italian, accent folding and stemming are not optional refinements, they are the difference between working and not." },

    { q: "What is the right escalation order when adding search to a PHP application?",
      a: ["MySQL FULLTEXT first, then Meilisearch, then Elasticsearch only for a reason you can name",
          "Elasticsearch first, since it scales furthest",
          "LIKE first, then Elasticsearch",
          "Whatever the team already runs, regardless of fit"],
      c: 0,
      why: "Each step adds an operational component with its own memory, backups and failure modes. FULLTEXT covers a surprising amount with no new infrastructure, and Meilisearch offers typo tolerance with far less operational weight than Elasticsearch." },

    { q: "Which fields belong in a search index?",
      a: ["Only those that are searched, filtered or displayed in results",
          "Every column of the source table, for completeness",
          "Only the primary key and the title",
          "Everything except large text fields"],
      c: 0,
      why: "Indexing everything inflates memory and reindex time for data nobody queries. The index is a query-optimised projection, not a copy of the database, and treating it as a second source of truth is how it drifts." },

    { q: "Why should index updates be dispatched to a queue?",
      a: ["Otherwise the search cluster's outage or slowness becomes your write path's outage",
          "Because search engines reject synchronous writes",
          "Because queued writes preserve ordering guarantees",
          "Because the ORM cannot write to an index synchronously"],
      c: 0,
      why: "Updating the index inside the request couples saving a product to a third-party service being healthy. On a queue, a search outage delays freshness and nothing else, and the backlog drains when it recovers." },

    { q: "Why does a bulk update in Eloquent often leave the search index stale?",
      a: ["Mass updates skip model events, so the indexing hook never fires",
          "Bulk updates run outside the transaction",
          "The index rejects updates without a version number",
          "Bulk updates always run on a replica"],
      c: 0,
      why: "Query-builder updates go straight to SQL and no model is instantiated, so nothing observes the change. Any bulk operation needs an explicit reimport afterwards, and remembering that is easier if it is written down beside the command." },

    { q: "What is the safest way to change an index's mapping or analyser in production?",
      a: ["Build a new index and swap an alias to it once it is complete",
          "Update the mapping in place and reindex gradually",
          "Delete the index and reindex during a maintenance window",
          "Add a second analyser and query both"],
      c: 0,
      why: "Mappings are largely immutable, and deleting means a window with no search at all. Indexing into a new name while the old one serves traffic, then repointing the alias atomically, gives a zero-downtime change and an instant rollback." },

    { q: "What is the most useful thing to log about your search feature?",
      a: ["Queries that returned zero results, because they are the synonym and catalogue gaps",
          "The average query latency only",
          "The most frequent successful queries",
          "The number of results per query"],
      c: 0,
      why: "Zero-result searches are users telling you what they expected to find and in what words. That log becomes your synonym list, your redirect rules, and occasionally a product decision. Nothing else in search analytics is as directly actionable." },

    { q: "A user searches for a product name they can see on the site and gets nothing. What should you check first?",
      a: ["Whether that document is in the index at all, and then whether the analyser matches",
          "Whether the search server has enough memory",
          "Whether the relevance weights need tuning",
          "Whether the query needs a wildcard appended"],
      c: 0,
      why: "The two common causes are sync failure and analyser mismatch, in that order. Confirming the document exists in the index separates 'the pipeline is broken' from 'the matching is wrong', and those have completely different fixes." },
  ],

  /* ------------------------------------------------------- chapter 22 --- */
  "22-queues-and-messaging": [
    { q: "What is the correct criterion for putting work on a queue?",
      a: ["The response does not depend on its result",
          "It takes longer than one second",
          "It calls an external service",
          "It writes to more than one table"],
      c: 0,
      why: "Slow is not the test. If the user must see the outcome, queueing it just moves the wait somewhere harder to report. Sending a confirmation email, generating a PDF, syncing to an ERP: the response is complete without them, so they belong on a queue." },

    { q: "Why must a job be dispatched after the database transaction commits rather than inside it?",
      a: ["The worker can pick up the job before the transaction commits and find no row",
          "Dispatching inside a transaction rolls the job back automatically",
          "The queue driver cannot participate in a transaction",
          "It doubles the job payload size"],
      c: 0,
      why: "The worker is a separate process racing your commit, and it usually wins. Laravel's afterCommit setting exists for exactly this, and the symptom without it is an intermittent 'model not found' that never reproduces locally." },

    { q: "Why must every queued job be idempotent?",
      a: ["Delivery is at-least-once, so a job can run twice after a timeout or a crash",
          "Because jobs run in parallel by default",
          "Because retries reorder the queue",
          "Because the payload may be corrupted in transit"],
      c: 0,
      why: "A worker that dies after doing the work but before acknowledging leaves the job visible again. If running twice sends two emails or charges twice, the design is wrong, not the broker. Guard with a unique key or make the operation naturally repeatable." },

    { q: "How should a job that fails because of malformed input be handled?",
      a: ["Fail it immediately without retrying, since retries cannot fix a permanent error",
          "Retry with exponential backoff like any other failure",
          "Retry indefinitely until an operator intervenes",
          "Requeue it at the back of the queue"],
      c: 0,
      why: "Retrying a permanent failure burns worker capacity and fills the log with noise that hides real problems. Distinguish transient from permanent in the job itself: back off on a timeout, fail fast on a validation error." },

    { q: "Why must a job's timeout be shorter than the broker's visibility timeout?",
      a: ["Otherwise the broker redelivers the job while the first attempt is still running",
          "Otherwise the job is deleted before it can finish",
          "Otherwise the worker cannot report progress",
          "Otherwise the retry counter never increments"],
      c: 0,
      why: "The broker assumes a job whose visibility window expired has died, and hands it to another worker. Two workers then run the same job concurrently, which turns an idempotency question into a live race on real data." },

    { q: "Why must queue workers be restarted on every deploy?",
      a: ["A long-running worker holds the old code in memory until it is told to stop",
          "The queue connection expires on deploy",
          "Failed jobs are cleared only on restart",
          "The autoloader cache is per-process and never refreshes"],
      c: 0,
      why: "Workers are long-lived PHP processes and do not reload files. Without queue:restart after a deploy you get the confusing situation where the website runs new code and background jobs run last week's, occasionally against a migrated schema." },

    { q: "Why separate queues by latency expectation?",
      a: ["So a slow bulk job cannot delay a time-sensitive one behind it",
          "Because a single queue cannot exceed a fixed throughput",
          "Because different job types need different serialisers",
          "Because failed jobs must be isolated per type"],
      c: 0,
      why: "One queue means one line. A nightly report generating for twenty minutes sits in front of the password-reset email somebody is waiting for. Separate queues with dedicated workers keep the fast path fast without any clever prioritisation logic." },

    { q: "Which two queue signals deserve alerts?",
      a: ["The failed-jobs table growing, and queue depth or job age rising",
          "Worker CPU usage and memory usage",
          "The number of jobs processed per minute",
          "The size of the job payloads"],
      c: 0,
      why: "An unwatched failed_jobs table is silent data loss: work that somebody requested and nobody performed. Rising depth or age is the early warning that consumption has fallen behind production, well before anyone reports it." },

    { q: "When is the database queue driver a reasonable choice?",
      a: ["Low volume, where avoiding another piece of infrastructure is worth the polling cost",
          "High volume, since the database is already tuned",
          "Whenever jobs must be transactional with application writes",
          "Whenever jobs must be delivered exactly once"],
      c: 0,
      why: "It is genuinely fine for a few thousand jobs a day on a small deployment, and it keeps the stack to one moving part. It scales poorly because polling and row locking contend with your application's own traffic on the same database." },

    { q: "What is a poison job, and what should the system do with it?",
      a: ["A job that always fails; after bounded retries it goes to a dead-letter or failed table for inspection",
          "A job carrying malicious input, which should be deleted silently",
          "A job that blocks the queue, which should be given a longer timeout",
          "A job that spawns other jobs, which should be rate limited"],
      c: 0,
      why: "Without a bound it retries forever, consuming workers and drowning the logs. Bounded retries followed by a durable record means the queue keeps flowing and a human can look at the payload and decide, which is the whole purpose of a failed table." },
  ],

  /* ------------------------------------------------------ chapter 22b --- */
  "22b-microservices": [
    { q: "What is the genuine, primary benefit of splitting a system into services?",
      a: ["Independent deployment by independent teams, which is an organisational benefit",
          "Faster page loads through parallel processing",
          "Lower infrastructure cost",
          "Simpler code in each repository"],
      c: 0,
      why: "The technical arguments are mostly secondary. What distribution buys is the ability for teams to ship without coordinating a release, and if your organisation is one team of five, you are paying the cost and collecting none of the benefit." },

    { q: "What happens to a page's latency when a monolith is split into services?",
      a: ["It increases, because in-process calls become network calls",
          "It decreases, because each service is smaller",
          "It is unchanged, since the same work is done",
          "It decreases only if the services are colocated"],
      c: 0,
      why: "A function call costs nanoseconds and an HTTP call costs milliseconds, plus serialisation and a new failure mode. Anyone claiming microservices will make the application faster has not measured, and saying so in an interview is a strong signal." },

    { q: "What is the largest hidden cost of distributing a system?",
      a: ["Losing the database transaction across a boundary",
          "The cost of running more containers",
          "Duplicated code between services",
          "Longer build times"],
      c: 0,
      why: "Inside one database, two writes either both happen or neither does. Across services you need a saga with compensating actions, and compensations are business decisions: what does it mean to un-reserve stock after the payment failed? That is not a coding problem." },

    { q: "Along which lines should a system be split?",
      a: ["By business capability, such as ordering or invoicing",
          "By technical layer, such as a data service and a UI service",
          "By programming language expertise on the team",
          "By table, so each service owns one table"],
      c: 0,
      why: "Splitting by layer produces services that must all change together for any feature, which is a distributed monolith with all the cost and none of the independence. A capability owns its data and its rules, so most changes stay inside one boundary." },

    { q: "How do you recognise a distributed monolith?",
      a: ["The services cannot be deployed independently and must be released together",
          "The services share a container registry",
          "The services are written in the same language",
          "The services call each other synchronously"],
      c: 0,
      why: "If a change requires coordinating three deploys in order, you have the network hops, the operational overhead and the debugging difficulty of distribution while still paying the coordination cost of a monolith. It is the worst position on the trade-off curve." },

    { q: "What is a modular monolith?",
      a: ["One deployable with enforced internal boundaries between modules",
          "A monolith split across several repositories",
          "A monolith with each module in its own database",
          "A monolith deployed as several identical instances"],
      c: 0,
      why: "You get the boundaries, the separated domains and the option to extract later, while keeping one transaction, one deploy and one debugger. It is the right default for the team sizes most of this market has, and extraction stays available." },

    { q: "How are module boundaries kept real in a modular monolith?",
      a: ["Enforced in CI by a tool such as deptrac that fails the build on a forbidden dependency",
          "By code review discipline alone",
          "By separate composer packages per module",
          "By naming conventions in the directory structure"],
      c: 0,
      why: "Boundaries that are not checked erode within months, one urgent fix at a time. A build that fails when the ordering module reaches into the invoicing module's internals turns an architectural intention into an actual constraint." },

    { q: "What is a defensible reason to extract one service from a monolith?",
      a: ["A single named force, such as an independent scaling profile or a separate compliance boundary",
          "That the codebase has grown large",
          "That microservices are the industry standard",
          "That the team wants to try a new language"],
      c: 0,
      why: "One force, one service. A PDF renderer that needs eight times the memory, or a component holding data under a stricter regime, is a reason you can state and evaluate. Size alone is an argument for modules, not for a network boundary." },

    { q: "An interviewer describes their microservice architecture and you think it is over-engineered. What is the best response?",
      a: ["Ask how they handle consistency across services",
          "Explain that a modular monolith would have been better",
          "Agree enthusiastically and say nothing further",
          "Ask why they did not choose a monolith"],
      c: 0,
      why: "You do not know their history, their team shape or their constraints, and criticising it costs you the room. The consistency question is genuinely interesting, shows you understand the real cost, and lets them tell you whether they have thought it through." },

    { q: "What is a saga, in the distributed-systems sense?",
      a: ["A sequence of local transactions with compensating actions when a later step fails",
          "A distributed lock held across services",
          "A two-phase commit coordinated by a broker",
          "An event log replayed to rebuild state"],
      c: 0,
      why: "Since there is no transaction spanning the services, you proceed step by step and undo what you can when something fails. The hard part is that a compensation is rarely a perfect inverse, so each one is a business decision written into code." },
  ],
});
